# Changelog

所有重要的更改都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.54.1] - 2026-08-19

### summarize_session 工具：历史摘要查询引导

- docstring 新增【历史摘要查询】引导：玩家咨询「之前/以前/上次/回顾/历史
  摘要」且未指明团或场次时，先 `action=status` 取团列表（含已结束的团），
  再 `action=summarize` + `campaign=团名`（必要时加 `session_seq`）读取；
- 明确提示：`campaign` 缺省只命中「当前进行中」的团，已结束的团必须显式
  传团名；
- `Args.campaign` 描述同步补充该约定；测试断言覆盖引导文本（防回归）。
  纯工具引导改动，不影响存储与命令行为。

## [0.54.0] - 2026-08-18

### 跑团记录（ADR-0029）：完整对话流记录 + 叙事式战报摘要

新增跑团记录功能：指令开始/结束记录一场跑团活动的完整对话流
（玩家消息 + 机器人回复），并把记录提取为叙事式战报摘要。

- **团/场次模型**：`/开始记录 <团名>` 存在则续写、不存在则新建；团内每次
  「开始 → 结束」为一个场次，同一团可跨多次开团累积（术语见
  CONTEXT.md「跑团记录」）；
- **完整对话流捕获**：`@filter.event_message_type(ALL)` 捕获玩家消息 +
  `@filter.on_decorating_result` 捕获机器人回复（含 LLM agent 的 DM 旁白）；
- **存储**：独立 SQLite `data_dir/trpg_log.db`（可变数据第二存储：追加式、
  按团/场次查询、摘要前取最近窗口）；升级插件不丢（在 data_dir 不在插件包）；
- **命令**：`/开始记录` `/暂停记录` `/继续记录` `/结束记录` `/删除记录`
  （写操作，群聊需白名单/管理员）；`/记录`（状态/看/摘要，全员可看）；
- **摘要**：`/总结 [团名] [场次] [重算]` —— 玩家骰指令写入时规则预标
  「结算」+ LLM（context.llm_generate）分「行动/吐槽」并生成叙事式剧情
  回顾 + 末尾结算统计；结果落库可回看，`重算` 强制刷新；记录过长时取最近
  窗口并注明；
- **LLM 工具**：第 10 个工具 `summarize_session`（summarize/status/view/
  start/pause/stop/delete，写操作工具内校验权限）；
- 配置项 `enable_session_log`（默认开）；`/帮助 记录` 查看语法；
  知识库构建、既有 KV 数据不受影响。

### 旧记录导入（/导入记录）：把既有团的记录倒灌进库

- `/导入记录 <团名> [文本]`：命令后直接粘贴纯文本记录（每行一条，支持
  `昵称: 内容` / `[时间] 昵称: 内容`），或上传 .txt/.log 文件（utf-8/
  gbk 自动识别）；
- 规则切分（`parse_transcript`）：按行 + 昵称/时间戳正则提取发送者、骰式行
  预标结算（复用结算启发式）；落为团内的一个新场次（不依赖记录状态、不打断
  正在 recording 的场次），随后 `/总结 <团名>` 直接生成摘要；
- 写操作，群聊需白名单/管理员；`session_log.import_session` 批量事务写入；
- `_extract_uploaded_file` 鸭子类型识别 File 消息段（`await seg.get_file()`）。

## [0.53.0] - 2026-08-18

### 职业选项补全（ADR-0028）：注法/奥术射击/元素戒律/符文/契约恩赐

对照 5etools optionalfeatures 枚举补齐 5 类此前缺失的可定制职业选项，
`optionalfeature` 从 190 条增至 241 条（11 类）：

- **奇械师注法**（AI，16 条）：TCE 独立文件，含奥能动力甲/增强武器/
  仿制魔法物品等（此前仅子职页残留 3 条 10 级增强注法，且不成选项）；
- **奥术射击**（AS，8 条）：XGE 魔射手放逐矢/欺诈矢/爆裂矢等，从子职
  正文段剥离；
- **元素戒律**（ED，17 条）：PHB 2014 四象宗寒冬之息/清流鞭等，
  `（需N级）` 标题内等级先决 → 先决字段（武僧等级N级+）；
- **符文**（RN，6 条）：TCE 符文骑士云雾/火焰/寒霜/岩石/山丘/风暴符文，
  山丘/风暴符文的 `（第N级或更高）` 先决入先决字段；
- **契约恩赐**（PB，4 条）：PHB 2014 链/刃/书之魔契（`####` 段）+
  TCE 符之魔契（护符魔契）。2024 三魔契早已作为魔能祈唤入库，不受影响。

提取管道 `scripts/optional_extract/` 新增 5 个源文件与 4 个特例提取函数
（裸标题/斜体行/#### 标题/带等级先决），同名重复条目自动合并去重。

### /筛选项 统一入口（/查祈唤 等 4 命令归并）

选项查询指令精简为 `/筛选项` 一个入口（原 /查祈唤 /查战技 /查修法
/查风格 删除，功能等价并入）：

- `/筛选项 <类型> <名称>` → 类型内详情（如 `/筛选项 注法 增强武器`）；
- `/筛选项 <名称>` → 全选项查详情，跨类型同名（如「刃之魔契」2014 契约
  恩赐 + 2024 祈唤两版并列展示）；
- `/筛选项 <类型>` → 该类型全部列表；`/筛选项 类型 <词>` / `先决 <词>`
  反查保留；
- 类型词支持简称：祈唤/战技/修法/风格/注法/射击/戒律/法门/符文/契约/
  恩泽/恩惠/血咒。

### 其他

- LLM 工具 `query_dnd_knowledge`：kind=选项 的 opt_type 中文取值扩展至
  11 类（新增 注法/奥术射击/元素戒律/符文/契约恩赐），schema 测试同步；
- 知识库重建（schema v12），optionalfeature 条目、feature_type/prerequisite
  标签全量更新。

## [0.52.0] - 2026-08-18

### 法术标签与概要重做（ADR-0027）

语义标签 `spell_keyword` 从 17 类（含减益/造物/战斗辅助/施法辅助）收敛为
**13 个效果域大类**：伤害/治疗/增益/控场/位移/防护/召唤/侦查/潜行/社交/
探索/幻术/即死（减益→控场、造物→召唤、战斗辅助+施法辅助→增益）；
库里只存大类名，全部旧细词/自由词（束缚→控场、火焰→伤害、亡灵→召唤…
99 个旧取值）经别名表归一，查询期 `标签 束缚` 等旧用法无缝兼容。

- **概要全量重跑**（1220 条）：≤30 字、含「效果+目标+代价」三要素；
  有确定性数值（伤害/治疗/临时生命）必写骰数（伤害类含骰率 94%），
  无数值可写的法术写效果描述。修复 85 条被「逗号截断」砍成残骸的概要
  （火球术等恢复完整骰数）与 53 条「豁免败」缩写。
- **法术状态反查启用**：`condition_inflict` 对法术从恒空变为 444 条/
  303 法术/16 状态（chm 纯中文正文状态词启发式）。
- **白捡元数据入库**（schema v12）：spells 表新增施法时间（动作 922/
  其他 154/附赠 117/反应 27）、持续时间原文、可施职业（JSON）三列；
  `spell_classes` 职业法术表改由 chm 中文职业优先（1204/1220 覆盖，
  英文查找表兜底），第三方书职业法术反查恢复。
- **射程规范化**：`_parse_md_range` 解析「自身（半径N尺）」「N里/英里」
  「视野」等中文范围文本，`/筛法术 射程=自身` 类筛选命中率提升。
- 测试 1273 passed；`/筛法术` 标签、`query_dnd_knowledge` docstring
  同步 13 类口径。

## [0.51.0] - 2026-08-15

### 修复：LLM 工具参数引导缺陷（ADR-0026）

修复 `query_dnd_knowledge` 查子职失败（如第三方法师子职「司书」THC）与
filter+选项 被拒：

- **docstring `kind` 枚举补「子职」**：此前 LLM 从工具 schema 看不到
  kind=子职 是合法取值，查子职只能猜「职业」（class）→ 0 命中；
  现在明确列出并注明「查子职能力用 class_features+subclass」。
- **用法提示新增「查子职」姿势**：`action=class_features + name=所属职业名
  + subclass=子职名`（例：name=法师 + subclass=司书），并说明 kind=子职
  的 detail/search 只能确认存在、完整能力走 class_features。
- **search/detail 全库兜底**：显式传 kind 但 0 命中时，自动不带 kind 全库
  再搜一次；命中其他类别（如子职）时返回候选并提示「可能存在其他类别
  同名条目」，引导 LLM 纠正。
- **子职空 body 引导**：`format_entry` 对 body 为空的子职条目，显示
  「子职条目本身无正文，请用 class_features+subclass 查询」，避免
  LLM 误判知识库无内容。
- **LLM 工具 filter 白名单补 `optionalfeature`**：此前 docstring 推荐
  「filter+选项」，实现却拒绝；现在 `/筛选项` 同款能力对 LLM 开放
  （filter+选项 + opt_type/opt_prereq 可查祈唤/战技/修法/风格等）。
- 新增回归测试 3 例（filter+选项 / 错误 kind 兜底 / 子职空 body 引导），
  全量 1273 通过。



### 新增：第三方职业选项入库（铳士战技项/邪狱使禁令恩惠/血猎手血咒）
- 铳士**子职 7 个**（v0.50.2 已有：密间客/技枪客/死眼客/白帽客/豪赌客/魔弹客/
  快枪客）确认齐全；新增其**战技项 6 条**（咬紧牙关/盲射/翻滚闪避等，featureType
  =战技 MV，/查战技 可查）。
- 新增**禁令恩惠 19 条**（邪狱使，featureType=IB）：`### N级禁令恩惠` 等级段
  → 先决「邪狱使等级N级+」，/筛选项 先决 7级 可反查。
- 新增**血咒 12 条**（血猎手，featureType=BC）：焦虑血咒/捆缚血咒等。
- `/查祈唤|战技|修法|风格` ftype 过滤为空时回退全部类型（禁令恩惠/血咒
  无独立命令也能查到并标注类型）；LLM 工具 opt_type 取值同步。
- 选项总数 153→190；全量 1270 通过。

## [0.50.2] - 2026-08-14

### 修复：第三方职业子职缺失（拳斗士/血猎手/邪狱使）
- 报障：拳斗士没有子职？排查发现 5e_chm 拳斗士书有 6 个子职文件
  （怒雷恶氓/惧恶之手/方圆擂尊/犬师拳狩/甜蜜科学/街角圣徒），血猎手 4 个
  （*结社）、邪狱使 5 个（影宗/恐惧屠戮者/狱言家/猩红骑士/筑灭师）——
  inventory 仅特判血族职业/铳士白名单，这三个书根目录的子职全漏。
- 修复：inventory 新增「第三方职业书根目录」判定（拳斗士/血猎手/邪狱使：
  主体.md=class、附属文件跳过、其余=subclass）；拳斗士书内「斗殴者」
  （怪物书）与邪狱使书内「家臣」（助战者规则）子目录跳过。
- 结果：+15 子职（拳斗士 6/血猎手 4/邪狱使 5），子职特性 1388→1470 行；
  `/查职业 拳斗士 怒雷恶氓` 等可查。
- 新增 tests/test_class_extract.py::test_inventory_third_party_class_books 回归；
  全量 1269 通过。

## [0.50.1] - 2026-08-14

### 修复：第三方职业（血族/拳斗士/铳士/血猎手/邪狱使）查不到
- 根因：5e_chm 提取的 class-*.json 缺 `class` 数组 → build_kb 未生成
  entries.kind='class' 条目 → editions/英文名/富化概要为空，2024 群规则下
  版本回退失效（「未找到该职业的特性数据」），/查询 广搜与 LLM 工具也查不到。
- 修复：emit 的 class 分支补 class 条目（英文名从 md 标题/frontmatter 提取，
  如「# 拳斗士 Pugilist」→ Pugilist）；finalize 按名合并时 chm 独有职业的
  class 条目补入（同名家不重复）。
- 效果：`/查职业 拳斗士` 在 2024 群规则下自动回退显示 2014 版并提示；
  `/查询 拳斗士` 广搜命中；职业头部显示英文名。
- 新增 tests/test_class_extract.py 回归（emit class 数组 / 英文名提取 /
  finalize 合并）；全量 1268 通过。

## [0.50.0] - 2026-08-14

### 可定制职业选项入库（魔能祈唤/战技/超魔法/战斗风格，ADR-0025）
- 新增知识库第七类「选项」（optionalfeature）：魔能祈唤/战技/超魔法/
  战斗风格 153 条，全部来自 5e_chm 人工校对中文，5etools 官方 featureType 枚举。
- 新命令：`/查祈唤 <名>` `/查战技 <名>` `/查修法 <名>` `/查风格 <名>`
  （卡片式：类型 + 先决/消耗行 + 正文 + 版本行，同名多版本并列）；
  `/筛选项 类型 祈唤` `/筛选项 先决 第5级`（类型/先决反查）。
- 2014 战斗大师战技从子职正文剥离入库（16 条全集）；2024 战斗风格专长
  补齐（5etools-cn 原缺该文件）。
- LLM 工具 query_dnd_knowledge 支持 kind=选项 + opt_type/opt_prereq 条件
  （40→42 参数）。
- 提取管道 `scripts/optional_extract/`：规则解析三格式标题 + 先决/消耗行，
  零 LLM 成本可复现。
- 测试 1265 通过；metadata/README/ARCHITECTURE/CONTEXT 同步。

## [0.49.0] - 2026-08-13

### 职业数据库从 5e_chm 全量重建（ADR-0024）
- `/查职业` 职业/子职数据源从 5etools-cn 换为 5e_chm 人工校对中文：
  职业 30 条（新增血族/铳士/血猎手/拳斗士/邪狱使等第三方职业）、
  子职 285 个（原 186 → 285，补齐官方扩展书子职 + 第三方子职）、
  本职特性 828 行 / 子职特性 2586 行（含 2024 版本职特性）。
- 修复「魔射手」查不到：子职查询在默认版本（群规则 2024）过滤后为空时，
  自动回退其他版本并标注（魔射手仅 XGE 2014 版）。
- 修复 2024 版本职特性被误标为子职「战士」：5e_chm 提取端（角色职业/职业/职业.md
  归 class）+ 5etools-cn 伪子职过滤（subclass 名=职业名）双修复。
- 规则引擎侧表（生命骰/豁免/施法进度/起始装备/职业富化概要）沿用
  5etools-cn 结构化 JSON（车卡硬数据不动）。
- 提取管道 `scripts/class_extract/`：inventory（归属推断）+ parser（规则解析，
  显式等级/####/裸标题/HTML 表多格式）+ emit（5etools 兼容 JSON）+ finalize
  （按名合并，chm 覆盖同名家 + 保留 cn 独有）+ llm_fallback（LLM 子代理兜底
  格式混乱的第三方文件，如胧忆岛落英结社/侵蚀行者）。

## [0.48.0] - 2026-08-13

### 新增

- **职业特性分层钻取（ADR-0023）**：`/查职业` 默认返回分层概要总表
  （第1~4层 = 1-4/5-10/11-16/17-20 级，每行「N级 名称：一句话概要」，
  基于 `class_features.summary` 全覆盖数据），替代原「；」串联的密集总表。
  - 第二参数新增钻取：子职（`战士 勇士`，原语法）、版本（`战士 2024`）、
    层级（`战士 第2层`）、等级段（`战士 5级` / `战士 3-7级`），解析优先级
    子职名精确匹配 → 版本 → 层级 → 等级段 →「特性」关键词。
  - `特性` 全文模式按层级段**分条发送**（每条消息一个层级段，带
    【职业·第N层 X-Y级】标题），替代原先一条 4000-9000 字符的整墙文本。
  - 单特性模式（`特性 动作如潮`）保持 v0.29.0 跨版本语义，单条输出。
- **版本策略（群规则感知）**：默认展示版本 = 群级开卡规则
  （`chargen_rule:{origin}` 的 edition，新增 `get_rule_edition`）→
  私聊/未设规则取最新版；显式版本参数可覆盖。目标版本无数据时自动回退
  另一版本并提示。
- **LLM 工具**：`query_dnd_knowledge` 的 class_features 默认返回概要层
  （token 友好），docstring 指引用 `feature="*"`/特性名/`class_level=N`
  取全文；40 参数 schema 不变。

### 修复

- **残留内联标签 `{@5etools …}`**（如「选择一个{@5etools 专长|feats.html}」原样
  显示）：`kb_tags` 标签名正则放开数字开头（`{@5etools 专长|feats.html}` → 专长），
  构建期与显示层双管齐下（显示层兜底清洗已发布旧库）。

### 变更

- 概要总表/全文分条的版本过滤：feature="*" 全量模式遵循默认版本（不再
  2014+2024 双版本各 4 层刷屏）；子职查询应用版本策略。

## [0.47.0] - 2026-08-13

### 新增

- **多重投掷（`N#`，ADR-0022）**：`/r 3#d20+d6` 重复投掷整个表达式 3 次，
  逐行输出 `#N 明细 = 值` 并汇总合计/平均；`#` 位置敏感消歧（左侧纯数字且
  在表达式开头才视为重复前缀，`d20+5#攻击` 的标签语义不变）。
  - 新配置项 `max_repeat_count`（默认 20）限制次数；全局骰数预算复用 `max_dice`。
  - 支持与成功计数组合（`3#3d6>3` 逐次独立计数）；多重投掷禁用 DC 与大成功/
    大失败判定；投掷历史记 1 条（摘要取标题行）。
- **复杂公式：四则运算 + 括号（ADR-0022）**：`/r 3d6*(2+4)d12`、
  `/r (2+3)d6`、`/r 3d(2*4)`、`/r (2d6+1)d8` 完整支持 `+ - * /`、任意嵌套
  括号、骰数/骰面位置的括号算式；除法向下取整、除零报错。
  - 表达式树（`ConstNode`/`DiceNode`/`BinOpNode`/`NegNode`/`GroupNode`）+
    扁平兼容回退，既有表达式与输出零回归；全角 `＃`/`（）＊／` 自动归一。

### 变更

- **计数组限根（行为收紧）**：带 `>N`/`<N`/`fN` 计数修饰的骰组不能参与四则
  运算或出现在括号中。此前 `3d6>3+1d4` 会静默忽略 `+1d4`，现改为明确报错。

## [0.46.0] - 2026-08-13

### 新增

- **怪物知识库全量重建（ADR-0021）**：以 5e_chm（= 不全书人工校对中文）
  全书为源、LLM 子代理批量提取怪物统计块转 5etools 兼容 JSON，全量覆盖
  官方核心书 + 36 模组 + MTG + 21 第三方书，怪物总数 5015 条，其中人工翻译
  4219 条（此前仅 1308），机翻条目从 3243 降至 796（仅剩 5e_chm 未覆盖的
  Plane Shift/UA/模组重印怪等）。
  - 提取契约 `scripts/llm_monster_schema.md` 覆盖三大格式族：2024/2025
    表格、2014 文本、MTG 变体；
  - 可复现管道 `scripts/monster_extract/`（inventory → make_plan → LLM
    子代理提取 → validate_monster_json 规则校验 → align 数值对齐 → finalize
    按名合并进 5etools-cn）；
  - 数值自动对齐 5etools-cn（修正 5e_chm 源 OCR 笔误，正文保留人工翻译）；
  - 新增第三方书怪物（万兽图志、塔尔多雷、鬼魅幽谷、德拉肯海姆、歪曲之月、
    狮鹫的鞍中珍宝Ⅱ 等 21 本，中文书名 source）。

## [0.45.0] - 2026-08-12

### 新增

- **怪物筛怪 6 维扩展（ADR-0020）**：/筛怪 新增伤害细分/状态免疫/速度/
  感官/阵营/特性名六个维度，全部怪物生效——
  - 伤害四类分开：`/筛怪 火焰伤害`（造成）、`火焰抗性`（抗性）、
    `火焰免疫`（免疫）、`火焰易伤`（易伤）；裸词「火焰」保持=火焰伤害
    向后兼容，结果底部提示可追加细分词；
  - 状态免疫：`/筛怪 震慑免疫`（「X免疫」伤害词表优先、未命中落状态，
    「毒素免疫」=伤害免疫、「中毒免疫」=状态免疫）；
  - 速度类型：`/筛怪 掘穴速度` `飞行速度` `游泳速度` 等；
  - 感官：`/筛怪 真实视觉` `黑暗视觉` `盲视` `震颤感知`（2014 译名
    「颤动感知」自动归一）；
  - 阵营：`/筛怪 守序善良` `混乱邪恶` `无阵营` 等（任意类/多阵营展开）；
  - 特性名：`/筛怪 再生` `魔法抗性` `传奇抗性` 等（trait 标题中文名，
    裸词自动反查）。
- **怪物 LLM 试点管道（ADR-0020）**：以 5e_chm/md 人工校对中文全书为源、
  LLM 子代理提取怪物数据转 5etools 兼容 JSON 走 build_kb 正式通道。
  试点：第三方书《火炬光下的克苏鲁》第七章 23 条神话生物入库
  （source=火炬光下的克苏鲁、edition=2024、人工翻译不标机翻），
  /查怪 /筛怪 /知识查询全链路可用；lore 页/章节页自动跳过；
  提取契约 scripts/llm_monster_schema.md + 规则校验器
  scripts/validate_monster_json.py 为管道标配（三层验收：自动校验/
  抽样对读/全量对账表）。此通道为后续覆盖更多第三方书/模组怪物的正式路径。

### 变更

- 构建期怪物标签新增 4 类 facet：speed_type（速度类型）、sense_type
  （感官）、alignment（阵营）、monster_trait（特性名）；entry_tags 通用表
  无需 schema 变更，重建 KB 即生效（SCHEMA_VERSION 不变）。
- build_kb 的 edition 判定支持条目自带 edition 字段（第三方 LLM 产物
  指定版本，5etools 源无该字段不受影响）。

## [0.44.0] - 2026-08-12

### 变更

- **法术详情改 PHB 卡片式显示（ADR-0019）**：/查法术 与 query_dnd_knowledge
  输出的法术卡从一行式「【法术信息】3环｜学派塑能｜…」改为玩家手册惯例排版——
  标题行纯净（`命令术｜Command`）、环位行（`一环 惑控（吟游诗人、牧师、圣武士）`）、
  施法时间/距离/成分/持续时间分行展示、正文与升环段分段；版本标注移到卡片底部
  （`版本：PHB·2014`），机翻/房规标记随之迁移。
- **概要与标签进入法术卡**：AI 一句话概要（`概要：…`）与语义标签
  （`标签：控制、惑控`，取自 entry_tags spell_keyword）显示在标题下、环位行前，
  缺省即跳过；私设（房规）法术同样生效。
- **环位行从结构化字段重建**：戏法显示「塑能戏法（…）」、仪式并入环位行
  （「一环 预言（仪式；…）」），规避第三方源 detail_meta 脏数据（如魔袋术）。
- **5etools 回退/私设路径同步**：`kb_build_lib._spell_body` 输出与 chm 同构
  卡片体（施法距离自身/触碰/视线、成分 V/S/M 字母带材料文本、持续时间
  专注/可提前消散渲染），`_kind_body` 新增可选 `classes` 参数。

### 修复

- `_summary_of` 跳过法术元信息行，避免无概要法术的搜索候选摘要显示成环位行。
- ARCHITECTURE.md 中 `KB_SCHEMA_VERSION` 陈旧笔误（"9"→7）修正。

## [0.43.0] - 2026-08-11

### 新增

- **法术知识库改用 5e_chm Markdown 主数据源（ADR-0018）**：法术条目中文详述/
  元数据从本地人工校对中文资料（`5e_chm/md`，htm→md 转换产物）重建，取代
  5etools-cn JSON 作为法术唯一来源。新增 `scripts/chm_parser.py`（速查表
  ×详述页 join 解析）、`scripts/audit_chm.py`（对账）、`scripts/gen_enrich.py`
  （富化补覆盖）；`build_kb.py` 新增 `--spell-md` 开关。
- **法术全量双版本收录**：2014 PHB + 2024 PHB + 全部扩展书 936 条官方 +
  第三方 284 条（合作方万法大全，黯潮/胧忆岛/歪月/克苏鲁等 16 家），共
  **1220 条**，同名多版本并存（2024 优先展示，`format_detail` 既有分支生效）。
- **职业法术表修复（spell_classes）**：构建补 `--en-spell-lookup`，2491 行/
  10 职业，`/筛法术 职业：xx` 与车卡法术推荐（spells_by_class）恢复可用。
- **富化字段全覆盖**：spells.summary + entry_tags.spell_keyword 官方/第三方
  全部 1220 条覆盖（既有 554 条 AI 生成保留 + 缺口 701 条规则生成含学派
  兜底词），反查零缺口。
- **同英文名双版本中文名归一**：2014/2024 译名不一致（如 造水/造水术）时
  统一为 2024 名、旧名入别名，任一中文名搜索均命中两版。

### 修复

- **法术 is_machine 统一为 0**：md 为人工校对源，不再有 ⚠️机翻标注。
- **自动标签改从 md 正文提取**：dmg_dealt/condition_inflict/spell_shape/
  spell_target 在 md 主源下照常生成（依赖 5etools entries 的旧逻辑已适配）。

### 变更

- 数据库 SCHEMA_VERSION 10 → 11（重建触发）。
- 法术详述/元数据字段：施法时间/距离/成分/持续时间原样入库（2014 空格
  数字与 2024 无空格并存）。

## [0.42.1] - 2026-08-11

### 修复

- **魔法变体正文 `{=字段}` 变量占位符未解析**（`/查物品 武器+1` 显示
  「你会获得{=bonusWeapon}的额外加成」等 50 条）：`build_kb.py` 新增
  5e.tools 变量语法 `{=字段}` 的构建期替换（如 `{=bonusWeapon}` → `+1`，
  取值三级：条目顶层 → 变体顶层 → inherits；字段缺失保留原文并告警）。
- **变体正文优先级颠倒**：顶层 entries（变体本身的平实翻译）现优先于
  inherits.entries（展开给具体武器的模板），救回「恶毒武器」等依赖
  `{=dmgType}`（需展开上下文、静态无法解析）的条目。
- **itemEntry 模板函数 `{{getFullImmRes item.resist}}` 未展开**（龙鳞甲等
  40 条显示 `{{getFullImmRes...}}`）：正则拆分修复，getFullImmRes 参数
  `item.字段` 现正确匹配并取条目抗性字段中文全名（如「强酸」）。

### 其他

- 重建内置知识库（`kb_data/dnd_kb.db`，schema_version=10 不变，全库无
  `{=`/`{{` 残留）。

## [0.42.0] - 2026-08-11

### 新增

- **DM 战利品发放/收回指令**：`/发放`（grant）直接向团队背包批量发放
  战利品，`/收回`（revoke）批量撤回（管理员/白名单权限）：
  - `/发放 治疗药水 3 价=5银 火球术卷轴 1` 一条命令完成多件发放，支持
    `重=X`（重量）、`价=X`（价值，支持 2金5银 写法）、`备注=X` 逐项
    属性归属（兼容 `w=/v=/note=` 短键）；
  - `/收回 火球术卷轴 1` 撤回发错的物品；收回走破坏性鉴权（群管理员/
    白名单），发放全员可用；私聊一律拒绝（队伍背包按群隔离）；
  - 新增 custom_prefix_route 前缀 `{p}发放`/`{p}grant`/`{p}收回`/
    `{p}revoke`。
- **/bag 全量批量**：`/bag add/rm/put/take` 支持一条指令多件物品
  （`/bag add 治疗药水 2 绳索 1`），单件保留原行为（数量必填等零回归），
  批量逐件原子、失败列明、成功 N 件/失败 M 件汇总。
- **manage_inventory LLM 工具 items 批量参数**：`action=add/remove/put/take`
  支持 `items(array)` 批量（元素 `{"item": 名称, "qty": 数量}`，add 可含
  weight/value/note；防御 JSON 字符串），与命令侧全量对齐；docstring
  schema 同步（8 参数）。

### 修复

- **全插件去 Markdown 输出（ADR-0017）**：QQ/onebot 不解析 Markdown，
  此前背包/商店/角色卡/先攻/车卡等输出中的 `**加粗**` 会原样显示。
  本次以纯文本为标准清理全部约 30 处输出（成对 `**`/`*`/`~~`、行首
  `>`/`#`），保留 `- ` 列表、`1. ` 编号、emoji、`×`/`→` 等纯文本排版
  与骰式表达式（`3d6>3`、`d20+5#标签` 的符号属语法不误删）；新增
  `test_no_markdown_output.py` 守卫防回潮。
- **角色卡文本导入兼容纯文本头**：卡名头正则原本只认 `📜 **名字**（版本）`，
  去 markdown 后 format_sheet 输出的 `📜 名字（版本）` 无法 round-trip
  再导入——现兼容「带 `**`（旧格式）或带 `📜` 前缀」两种写法，宽松键行
  不受影响。
- **安全对称（ADR-0016）**：manage_inventory 工具侧 `remove` +
  `to_party=true` 从队伍背包撤回物品时引入与 `/收回` 同口径的
  `_check_destructive_permission` 鉴权（此前工具无鉴权可绕过命令侧收回
  权限）；put/take/add(to_party) 保持开放。

## [0.41.2] - 2026-08-11

### 新增

- **`/背包` 中文别名文档化**：`/bag` 的「背包」别名其实早已注册（`/背包`、`/inventory` 一直可用），本次让提示可见：
  - `/帮助` 菜单背包组命令名显示 `/bag /背包`；
  - `/帮助 背包` 与 `/bag` 空背包提示首行新增「别名：/背包、/inventory 等同 /bag」；
  - README 快速上手表与背包章节新增「指令别名」说明。

### 修复

- **自定义前缀路由补齐全部命令与别名**：`custom_prefix_route` 此前只匹配部分命令，导致设置自定义前缀（如 `.`）后若干指令失效。本次按 `@filter.command` 注册清单全量比对补齐：
  - 整条缺失：`dnd`（属性生成）、`筛背景`（含 `bfilter`/`背景筛`）；
  - 别名缺失：查系列中文别名 `怪物`/`物品`/`专长`/`背景`/`状态`/`种族`、`查询` 的 `q`、筛系列 `筛怪物`/`筛魔法`/`筛道具`/`筛血统`、`帮助` 的 `commands`/`cmds`；
  - `dnd` 重构为 `_handle_dnd(event, arg, display_prefix)`，自定义前缀下错误提示跟随前缀显示（如 `.dnd abc` → `用法：.dnd [组数]`）；
  - `rprefix` 有意不进路由（设置前缀的命令本身始终经系统 `/` 前缀可用）。

## [0.41.1] - 2026-08-11

### 修复

- **焰舌等魔法武器完全查不到**：5e.tools 新数据模型把可附着于多种武器的魔法效果
  （焰舌/霜铭/+N 武器等，214 条）从 items.json 移入 `data/magicvariants.json`，
  而下载脚本清单漏了该文件、构建脚本也无展开逻辑。本次：
  - `fetch_cn_data.py` 下载清单补上 `data/magicvariants.json`；
  - `build_kb.py` 将变体**本体**作为一条 item 入库（`/查物品 焰舌` 命中
    「焰舌」，XDMG·2024 珍稀需同调，正文完整），所有可能展开名（焰舌长剑、
    Flame Tongue…）注册为别名指向本体，不生成大量「焰舌长剑/巨剑」条目；
  - 匹配逻辑对齐 5e.tools 官方 render.js（edition/requires/excludes，
    经官方搜索索引 2777 个展开名双向零差异验证）；
  - 顺带解析 `{#itemEntry 抗性护甲}` 裸引用（items-base.json itemEntry 模板
    展开 + `{{item.resist}}` 等变量填充），消除 100+ 条物品正文残留标签。
  - 新增「魔法变体」物品类型码（GV）。

## [0.41.0] - 2026-08-11

### 新增

- **角色卡每一项都可单独设置**（`/卡 设` 与 LLM 工具 `manage_character` set 同步生效）：
  - **六维属性**：`/卡 设 力量 18`、`/卡 设 敏捷 99`（越界自动 clamp 1-30），
    支持中文名/英文缩写/全称别名（力量/敏捷/体质/智力/感知/魅力、
    str/dex/con/int/wis/cha、strength…）。此前导入后无法单独修正属性值，
    这是本次修复的核心缺口。
  - **种族**：`/卡 设 种族 半精灵`（短文本，≤40 字）。
  - **职业**：`/卡 设 职业 战士 3 + 法师（塑能） 2` 整体替换兼职列表
    （含子职与等级，`-` 清空职业），解析与文本导入共用同一函数
    （`character.parse_classes_text`，单一事实来源）。
  - **规则版本**：`/卡 设 版本 2024`（兼容 5e/5.5e/5r/5.0 写法，无法识别不应用）。
  - 设置六维属性/职业/版本后**自动触发规则引擎重算**：体质变化联动 HP base、
    敏捷变化联动先攻/AC（轻甲）、力量/敏捷变化联动攻击加值、版本变化按新版
    规则重算战斗字段；房规 bonus 层原样保留（`/卡 设 hp 25` 等仍只写 bonus）。

### 修复

- `/卡 设 力量 99` 此前报「未知字段」——力量/敏捷等六维属性不在字段白名单内，
  现在可正常设置（测试 `test_set_unknown_field` 改用真正的未知字段验证报错路径）。

### 内部

- `card_import.py` 的职业段/版本归一复用 `character.parse_classes_text` 与
  `character.normalize_edition`，删除重复正则与 `_norm_edition`。

## [0.40.2] - 2026-08-11

### 修复

- **怪物施法漏渲染「每项N/日」**：5etools-cn 的每日次数施法挂在 spellcasting
  **顶层 `daily`**（874 只怪物，如奥喀斯「每项3/日：解除魔法」），此前代码只在
  `spells` 各环阶分组里查找 `daily`（源数据无此形态，属死代码），导致只显示
  「随意施展」。修复后奥喀斯、巫妖等按 statblock 习惯输出完整施法：
  ```
  随意施展：侦测魔法
  每项3/日：解除魔法
  每项1/日：时间停止
  ```
- **顺带补全**：`charges`（充能施法，奥喀斯之杖「1充能：死亡法阵、死亡一指」）、
  `rest`/`restLong`（休息后施法）一并渲染；`daily` 次数键带 `e` 尾缀
  （5e.tools N/day each 记法，如 XMM 巫妖 `2e`/`1e`）显示时去尾缀并按次数降序；
  环阶内 `daily` 保留防御处理（兼容私设条目）。

## [0.40.1] - 2026-08-11

### 修复

- **职业法术表补全扩展书法术（spell_classes 1190 → 1463）**：`_load_en_spell_classes`
  从只取 `class` 改为合并 `class` + `classVariant`。5e.tools 语义中，扩展书
  （XGE/TCE/FTD/BMT 等）对既有职业列表的增补法术只挂在 `classVariant` 下
  （如 XGE「吸收元素」→ 德鲁伊/游侠/术士/法师 + TCE 奇械师），此前漏挂
  约 99 条扩展书法术的主职业归属，`/筛法术 职业 <职业>` 反查不到。
- 剩余 16 条未匹配为 EGW/GGR **子职限定法术**（如「暗黑星辰」只属于时间/
  重力法师子职、GGR 公会法术），按规则不进主职业表，属预期；`subclass`
  /`feat`/`race` 维度仍不在本期范围。

## [0.40.0] - 2026-08-11

### 修复

- **怪物体型中文化**：`/查怪` 等输出的「体型M/体型G」改为「中型/巨型」等
  中文（`SIZE_CN_REV` 映射；多体型列表用「或」连接），与种族显示一致。

### 新增

- **怪物类型行 + 阵营显示**：`【数据】` 头部新增「体型+类型+阵营」段，
  贴合玩家惯用的 statblock 样式：
  - 类型：`中型不死生物（法师）`——5etools 英文类型码 → 中文
    （`MONSTER_TYPE_CN_REV`），2024 型 `{"type","tags"}` 结构自动带上
    「（法师）」式标签；
  - 阵营：5e 字母码 → 中文（守序善良/中立邪恶/任意邪恶阵营等，规则与
    5etools-cn 站点 js/parser.js 一致，含 NX/NY 特殊码与概率形态）；
    **数据缺失时显示「不固定阵营」**。
- **挑战等级行附 XP/巢穴/PB**：`挑战等级CR21（XP33,000，或巢穴内41,000；PB+7）`
  ——XP 按 CR 查 2014 经验表，巢穴段支持 2014 `cr.lair`（按巢穴 CR 查 XP）
  与 2024 `cr.xpLair`，PB 按 CR 查熟练加值表。
- **AC/HP 标签与数值加空格**：`AC 20（天生护甲）｜HP 315（42d8 + 126）`，
  与速度/挑战等级段排版一致。
- **`scripts/fetch_cn_data.py`（开发脚本）**：直连下载 5etools-cn 数据到
  本地目录（GitHub git trees API + raw 文件，约 20MB，只拉 build_kb 需要的
  文件），commit 默认取内置库 `meta.source_commit`（零数据漂移），替代
  易被全局 git 代理掐断的 clone + sparse-checkout 流程；配合既有
  `scripts/fetch_en_spells.py`（英文法术源查找表）即可复现完整构建。

### 数据

- 知识库随包库重建：数据源仍为 5etools-cn `884d65a`（data_version 后缀
  更新为 20260810），各 kind 计数与 v0.39.0 完全一致（怪物 3741、法术 554、
  职业法术表 spell_classes 1190 等），**仅怪物 body 渲染变化**（上三项）。

## [0.39.0] - 2026-08-10

### 新增

- **`/商店 清空`（管理员）**：一次清空当前会话商店的全部商品条目，
  回购系数保留（与 `/商店 初始化` 语义一致：清商品不清配置）；空店提示
  「商店本来就是空的」。权限与其他商店管理操作一致（群聊白名单/管理员、
  私聊放行）。别名 `clear`/`clr`/`清除`。
- **商店批量操作（命令 + LLM 工具双入口）**：
  - **批量买/卖**：`/商店 买 长剑 匕首 2` = 长剑×1 + 匕首×2。语法为
    「名称 [数量]」交替、**数量可省略（默认 1）**，数字 token 归属前一个
    物品——完全向后兼容既有单件写法（`买 长剑`、`买 长剑 2` 行为不变）；
    数字出现在名称位报「数量前缺少物品名称」。
  - **批量上架**：`/商店 上架 长剑 价=2金 匕首 库存=3` 逐项属性归属
    （`价=`/`库存=` 归当前物品，不写则库价/无限）。
  - **批量下架**：`/商店 下架 长剑 匕首` 一次移除多件。
  - **失败语义**：批量逐件原子（复用单件事务，锁序 Shop→Inventory 不变），
    某项失败（不在架/库存不足/钱不够/已在架）不影响其余，结果逐行明细 +
    「成功 N 件 / 失败 M 件」汇总。
  - **LLM 工具 `manage_shop`**：新增 `items(array)` 批量参数
    （`[{"item":"长剑","qty":1},…]`，上架可带 `price`/`stock`；兼容旧
    `item`/`qty` 单件参数，`items` 缺省时回退；防御数组被序列化成 JSON
    字符串）。动作扩为 `list`/`buy`/`sell`/`add`/`remove`/`clear`——
    管理动作在工具内做 `_check_destructive_permission` 鉴权，非管理员
    拒绝并引导找 DM（修订 ADR-0010 原「管理操作不暴露给 LLM」约定）。
- 决策与边界详见 `docs/adr/0015-shop-batch-and-clear.md`。

## [0.38.0] - 2026-08-10

### 新增

- **`/dnd [组数]` 指令（DND 5e 属性随机生成）**：按 5e 标准掷骰开卡规则
  生成角色属性——每组掷 6 次 `4d6kh3`（4 颗 d6 取最大 3 之和），得到 6 个
  属性值，可直接分配填入六维；组数默认 1、上限 20（防刷屏，模块级常量
  `_DND_MAX_GROUPS`，不新增配置项）。输出紧凑（每组一行纯数字，不展示
  4d6 明细；需要明细/写草稿请走 `/车卡` 的掷骰开卡），成功时以 `dnd N`
  写入投掷历史。掷骰复用 `_roll_chargen`（走插件骰子限制）。结果不自动
  写入角色卡（独立生成指令）。
- **中文「优势/劣势」紧贴后缀语法（`d20优势` / `d20劣势` 系列）**：
  `/r d20优势`、`/r d20优势+2`、`/r d20优势+1d4`、`/r d20劣势` 等写法
  等价于既有 `d20adv` / `d20dis`（掷 2 枚 d20 取大/取小，也可用于任意
  骰面如 `1d4优势`=2d4kh1）。实现为命令层预处理（`_do_roll` 内
  `_map_zh_adv_dis` 把紧贴骰式的「优势/劣势」映射为 adv/dis），因此
  `/r`、自定义前缀（`.r d20优势`）、`roll_dice` LLM 工具三个入口自动
  全部生效，且不修改 dice_parser 上游语法。边界：**必须紧贴**——带空格
  （`d20 优势`）、前置（`优势d20`）、单独使用（`优势`）均报语法错误，
  避免「以为是优势实际没加」的静默失误；标签内含「优势」二字（如
  `d20 战斗优势`）不受影响；玩家登记同名命名掷骰（`/卡 骰 d20优势 …`）
  时按既有优先级（角色卡联动优先）。决策与取舍详见
  `docs/adr/0014-zh-adv-dis-command-layer.md`。

## [0.37.0] - 2026-08-09

### 新增

- **私设助手（manage_homebrew，第 9 个 LLM 工具）**：DM 把纯文本私设交给
  助手即可转成合法私设 JSON，可选直接落盘，并能对照已有条目点评平衡性：
  - **转录（convert，默认）**：LLM 生成简化格式 JSON 后回传工具做双程校验
    （临时目录走 HomebrewManager 权威解析，与 /kb reload 零漂移），返回
    「通过/条目数/逐条告警」；通过后全文贴回 + 字符数统计，超过约 1200
    字符提示开写入或分条转换。撞官方 `(kind,name,source)` 键的条目醒目
    标注「将作为房规覆盖」。
  - **写入（write）**：新配置 `homebrew_write_enabled`（bool，**默认关**）
    开启后可用；权限要求白名单/管理员（**私聊同样受限**——私设是全局
    数据）。文件名可指定（安全化，拒绝路径分隔符）或从 source 派生；
    目标已存在时默认拒绝并列出存量条目，DM 确认后 `overwrite`（整文件
    替换）或 `merge`（按 kind+name+source 合并、同键新盖旧）重试。落盘
    为「临时文件 + os.replace」原子写，写后自动 `reload_homebrew()` 生效。
  - **点评（review）**：解析草稿返回结构化锚点（kind/name/source/环级/
    稀有度/CR 等 + 同名条目命中）；对照材料由 LLM 自主调
    `query_dnd_knowledge` 获取，docstring/工具守则/返回尾句三重强制
    「先查库再点评，禁止仅凭记忆」。
  - **新模块 `homebrew_writer.py`**（纯函数）：`validate_homebrew_text` /
    `flatten_raw_entries` / `sanitize_filename` / `derive_filename` /
    `merge_homebrew_texts` / `atomic_write_text`；kb.py 新增
    `homebrew_dir` property 与 `official_key_set()`。
  - 工具守则（_llm_request_guard）追加私设句，覆盖全部 9 个工具。

### 边界说明

- 私设职业/子职仍不参与规则引擎联动（v0.36.0 既有边界）。
- 决策与取舍详见 `docs/adr/0013-homebrew-llm-write-tool.md`（含「为什么
  插件自写文件而不用 AstrBot 内置文件工具」）。

## [0.36.0] - 2026-08-09

### 新增

- **运行期私设（房规）数据（Homebrew Overlay）**：DM 无需改代码、无需重建
  数据库，即可给知识库添加自己的私设条目：
  - **目录约定**：`{AstrBot data_dir}/trpg_homebrew/*.json`（文件在 data_dir
    不在插件包内，升级插件不丢、换机器复制即走、DM 之间直接分享文件）。
  - **双格式**：5etools 社区标准 JSON（顶层键=条目类型，正文走 entries/
    trait/action 等字段）与简化格式（条目内写 kind/name/source/body，纯文本
    正文最易上手）；9 类全支持（法术/怪物/物品/专长/背景/状态/种族/职业/子职）。
  - **命令**：`/kb reload` 热重载私设目录（不用重启）；`/kb 私设` 查看概况
    （文件数/条目数/覆盖数）。帮助文本同步。
  - **合并语义**：私设 source 独立 → 纯新增；与官方 `(类型,名称,来源)` 相同
    → **覆盖官方（房规修正）**。查询层 search/detail/filter 结果私设置顶并
    标注 `🏠房规`；LLM 工具 `query_dnd_knowledge` 走同一查询层自动可查私设
    （带标注，防 LLM 混淆官方/房规）。
  - **标签反查**：条目可选 `tags` 字段（官方 facet 词表）参与 `/筛X` 反查。
  - **健壮性**：坏文件/坏条目只跳过并记录，绝不影响插件运行；reload 原子替换。
  - **示例**：插件包内新增 `homebrew_examples/` 目录（法术/物品/怪物/职业各一，
    两种格式都有），复制到 `trpg_homebrew/` 后 `/kb reload` 即可体验。

- **共享构建库 `kb_build_lib.py`**：把 build_kb.py 的「5etools 条目 → 正文/
  侧表」渲染链与提取工具（`_kind_body` 全家桶、`is_machine_entry`、数值解析、
  `_item_value_weight`/`_item_combat_cols`/`_ability_payload`）抽为独立模块，
  构建期与运行期私设加载共用同一套逻辑（零漂移，已用真实 5etools 数据 184 项
  对比验证一致）。

### 边界说明

- 私设职业/子职目前只支持名称查询与正文展示，不参与规则引擎（车卡/HP/法术位）
  联动；简化格式缺结构化字段时对应维度（环级/CR/稀有度等）筛选不命中，但名称
  查询与全文展示不受影响。

## [0.35.0] - 2026-08-09

### 新增

- **构筑咨询（advise_build，第 8 个 LLM 工具）**：把「目标/关键词」与「角色卡
  现状」确定性组装成候选档案（职业/种族/背景/子职/专长/法术/升级特性时间线），
  LLM 只负责基于档案组织话术，杜绝凭记忆编造条目名：
  - **两个 action**：`new_build`（从零构筑，含「根据背景推荐」——LLM 提炼
    主题词传入 goal）与 `level_up`（插件直读活跃角色卡，LLM 零传参）。
  - **goal 自由文本消歧**：别名归一（"前排"→坦克）+ 查库确认 facet 归属
    （一个词可命中多家族），整词未命中做 CJK 复合词抽取（"前排打手"→坦克）；
    `keywords` 参数供 LLM 精准补充。
  - **防幻觉输入侧约束**：候选全部来自知识库反查（带 AI 一句话概要+版本标注），
    docstring 与 `_llm_request_guard` 明文「推荐条目必须来自工具返回，禁止
    凭记忆补充条目名」；查全文走 query_dnd_knowledge detail。
  - **专长前置「标注不过滤」**：level_up 逐条标注 ✅满足/❌缺力量13/⚠️未校验
    （prereq_ability/race/feat/feature 四类），LLM 可顺势建议属性规划。
  - **特性时间线**：每职业下一级特性（等级+名称+一句话概要），按卡面版本去
    双版本行；全文由 LLM 自行调 query_dnd_knowledge 查。
  - **版本过滤**：new_build 读群规则 ChargenRule.edition；非空时仅取该版本
    条目。
  - **升级联动**：guide_chargen start 新增 race/class_name/background 预填
    参数（值必须来自 advise_build 档案），走状态机既有校验，合法跳步/非法
    回退正常询问（2014 全预填→属性步；2024 物种写入 species）。

- **职业法术表（schema v10）**：新侧表 `spell_classes(entry_id, class_name)`
  存法术→主职业（英文 5e.tools 生成的法术源查找表
  `gendata-spell-source-lookup.json` 按 ENG_name+source 匹配；构建脚本新增
  `scripts/fetch_en_spells.py` 下载 + build_kb `--en-spell-lookup` 参数，
  未命中写 `spell_classes_unmatched.json` 报告不阻塞构建）：
  - **三入口暴露**：`/筛法术` 前缀词「职业」、query_dnd_knowledge 新增
    `spell_class` 参数（中英文职业名均可）、advise_build 内部使用。
  - 子职/领域附赠法术不在本期范围（保持主职业表）。

- **query_dnd_knowledge 新增 2 参数（38→40）**：`spell_class`（职业法术表
  反查）与 `class_level`（class_features 等级过滤，如「野蛮人 7 级获得
  什么」→ action=class_features + class_level=7）。

- **kb.py 新查询能力**：`class_features` 加 `level_min/level_max` 等级区间
  过滤；新增 `spells_by_class`/`feat_prereq_facets`/`value_facets`/
  `entry_tags_of`；filter() 加 `spell_class`/`level_max` 参数；KbEntry 带出
  `entry_id`（前置标注用）。

### 修复

- filter() 无标签时专长/背景/职业/子职返回全库条目的既有缺陷（无能力标签的
  兜底查询场景暴露，补 e.kind 守卫）。

### 测试

- 新增 `tests/test_build_advisor.py`（22 例：分词/复合词抽取/法术环上限/前置
  标注/档案结构/版本过滤/时间线去重/法术建议）、`tests/test_advise_build_tool.py`
  （12 例：new_build/level_up/无活跃卡/ContextWrapper 注入/预填透传/spell_class
  /class_level）；test_build_kb.py 加 spell_classes 构建用例（fixture
  `en_spell_lookup.json`）、test_llm_tool_schema.py 更新至 40 参数+advise_build
  断言+guide_chargen 预填参数断言、test_chargen.py 加预填 4 例、test_kb.py 加
  新查询 6 例。全量 987 通过。

## [0.34.0] - 2026-08-09

### 新增

- **种族/背景富化：AI 一句话概要 + 能力标签反查**（仿专长/法术/职业的富化
  通道，schema v9）：
  - **AI 概要 + 标签**：种族 160 条 + 背景 148 条全覆盖（同名多版本分别
    生成），每条带一句话概要（`races.summary` 列 / 新建 `backgrounds` 侧表）
    与 3-8 个能力标签（`race_keyword`/`background_keyword` facet），源
    `kb_patches/race_enrich.json`（160 条）与 `background_enrich.json`
    （148 条），构建期合并。
  - **种族标签词表**（`RACE_KEYWORD_TAGS`，7 类）：属性倾向/战斗方式/
    防御生存/机动/技能倾向/主题风味/特殊机制；收编 5etools 官方 traitTags
    （天生武器/天生护甲/水陆两栖/魔法抗性/强力构筑/修整强化/日照敏感），
    主题词与子职词表同源。与既有 facet 互补（体型/速度/抗性/天生施法法术名
    已结构化，不重复打标）。
  - **背景标签词表**（`BACKGROUND_KEYWORD_TAGS`，5 类）：属性倾向/技能倾向
    （18 技能全收，背景技能是灵魂）/身份主题（39 出身词）/工具装备（17 工匠
    工具 + 6 kit + 赌具/乐器/交通工具，canonical 沿用 5etools-cn 译名）/
    特殊机制（起始专长/组织/财富/地位…）。
  - **/筛种族 扩展标签反查**：裸词自动消歧（「变形/水陆两栖/魅力」→
    race_keyword；「火焰/光耀」等伤害词仍优先走天生抗性 dmg_resist），
    前缀词「标签/关键字/关键词」显式指定。
  - **新命令 /筛背景**（bfilter/背景筛）：裸词技能/身份/工具/起始专长反查，
    如 `/筛背景 隐匿 盗贼工具`、`/筛背景 贵族`、`/筛背景 标签 起始专长`
    （背景此前只有 /查背景，无筛指令）。
  - **/查种族 /查背景 头部展示 AI 概要**；筛选列表/搜索候选优先用 AI 概要。
  - **LLM 工具 query_dnd_knowledge 36→38 参数**：新增 `race_keywords` /
    `background_keywords`（逗号分隔多标签 AND，别名归一），供 LLM 结合
    角色卡/团情给出种族/背景选取建议。

### 修复

- 知识库 schema v9（`races.summary` 列 + `backgrounds` 侧表），运行期
  版本检查自动回退旧库逻辑不变。



### 新增

- **职业/子职富化：AI 一句话概要 + 能力关键字 + 职业定位**（仿专长/法术的
  富化通道，schema v8）：
  - **职业定位**：13 个基础职业 + TCE 协力者按四大定位归类（武者：战士/野蛮人/
    武僧；奥法：法师/术士/魔契师；神职：牧师/圣武士/德鲁伊；专家：游侠/游荡者/
    吟游诗人/奇械师），`/查职业` 头部展示「定位：X」，可用 `/筛职业 武者` 反查。
  - **AI 概要 + 关键字**：职业 29 条（含 TCE 协力者，排除 UA 秘术师）+ 子职
    186 条（排除 UA/Plane Shift 跨界）每条带一句话概要（`classes` 侧表）与
    3-6 个能力关键字（`class_keyword`/`subclass_keyword` facet），源
    `kb_patches/class_enrich.json`（215 条全覆盖，构建期合并）。
  - **新词表**：`CLASS_KEYWORD_TAGS` 六类（战斗方式/施法/辅助/技能倾向/属性依赖/
    特殊机制，canonical 与专长/法术词表对齐）+ `SUBCLASS_KEYWORD_TAGS` 三类
    （定位倾向/主题风味/特色机制，含八大魔法学派）；别名归一（潜行→隐匿、
    重武器→双手、塑能学派→塑能…）。
- **`/筛职业` 命令**（别名 `cfilter`/`职业筛`，自定义前缀同步生效）：
  - 定位反查：`/筛职业 武者`、`/筛职业 定位 奥法`；
  - 能力标签反查（裸词自动消歧 + 前缀词「标签/关键字/关键词」）：`/筛职业 近战 爆发`
    （组合 AND）、`/筛职业 奥术施法 智力`、`/筛职业 标签 治疗`；
  - 结果列表展示 AI 概要（class_summary 优先于正文截断）。
- **`/筛子职` 命令**（别名 `sublass_filter`/`子职筛`，自定义前缀同步生效）：
  - `/筛子职 治疗 神圣`、`/筛子职 塑能`、`/筛子职 标签 召唤`；
  - 结果列表展示 AI 概要。
- **LLM 工具 `query_dnd_knowledge` 3 新参数**（36 参数）：`class_role`（职业定位）、
  `class_keywords`（职业能力标签，逗号分隔 AND）、`subclass_keywords`（子职能力
  标签，逗号分隔 AND）；filter 打开对 kind=职业/子职 的支持，返回条目带一句话概要，
  供 LLM 结合角色卡/团情给出职业/子职选取建议。`kind` 新增「子职」中文取值。
- **`/查职业` 头部富化**：展示「定位：武者」「概要：…」（classes 侧表带出）。

### 变更

- 知识库 schema v7 → v8：新增 `classes(entry_id, summary, role)` 侧表；
  `entry_tags` 新增 `class_keyword`/`class_role`/`subclass_keyword` facet。
  旧库自动回退内置库逻辑不变（resolve_db_path 版本检查）。
- 自定义前缀路由补上既有遗漏：`ffilter`/`专长筛`（筛专长的前缀别名此前
  未注册到前缀路由）。

## [0.32.0] - 2026-08-09

### 新增

- **命名掷骰接入 `/r` 联动 + 全套增删改查**（此前 `named_rolls` 只登记不消费，
  是半成品字段）：
  - **`/r <名称>` 直接用登记表达式掷骰**：`/卡 骰 侦察 1d20+2` 后
    `/r 侦察`、`/r 侦察 15`（DC 追加）即按 `1d20+2` 掷并标注
    「命名掷骰（卡名·「侦察」）」；`/r` 命令、自定义前缀、LLM 工具
    `roll_dice`（模型说「掷侦察」）三入口同步生效。
  - **命名优先于内建别名**：登记名与属性/技能/豁免/攻击别名冲突时（如
    `/卡 骰 察觉 1d20+8`），`/r 察觉` 用登记表达式（玩家显式意图覆盖
    默认联动）；不传命名掷骰时行为与旧版完全一致。只在整词命中时触发，
    紧凑写法不受影响。
  - **删除**：`/卡 骰 <名称> -`（全角 `－`/「删」亦可）；LLM 工具
    `manage_character` 的 `named_roll` 动作 value 形如「名称=-」删除。
  - **查看**：`/卡 详情 掷骰` 列出全部命名掷骰（名称 + 表达式）。
  - 增/改沿用 `/卡 骰 <名称> <表达式>`（同名覆盖）。

## [0.31.0] - 2026-08-09

### 新增

- **细项条目级删除（角色卡全套增删改查补齐第一步）**：
  - **攻击条目删除**：此前 `/卡 设 攻击` 只能新增/覆盖同名。现在
    `/卡 设 攻击 <名称>=-`（全角 `－`/「删」亦可）删除该攻击条目，
    `manage_character` 工具新增 `del_attack` 动作（field=攻击名）。
    装备武器名 /「{职业}法术攻击」等规则引擎**自动生成**的条目删除时会提示
    「重算后会恢复，要彻底移除请先清空对应装备槽」（生成集由重算再生，
    玩家自建条目删除后不复活）。
  - **已知法术单条增删**：`/卡 法术 加 <环阶> <法术名>`、`/卡 法术 删
    <环阶> <法术名>`（环阶支持 戏法/0环/cantrip、一环/1环/1 … 九环/9环，
    环空自动移除）；`manage_character` 工具新增 `add_spell` / `del_spell`
    动作（field=环阶，value=法术名）。整体覆盖式 `/卡 设 法术` 保留。
  - 命名掷骰（`/卡 骰`）暂未改动：它目前只登记不消费（`/r` 联动未接入），
    是否补齐删除/查看或真正接入掷骰联动另行确认。

## [0.30.0] - 2026-08-09

### 新增

- **角色卡补齐玩家纯文本卡字段**（对照玩家常见卡面逐项核对）：
  - **人物基础信息**：信仰 / 年龄 / 性别 / 身高 / 体重 五个自由文本字段，
    卡面新增「人物：性别 女　年龄 14　身高 148cm　体重 40kg　信仰 无」行，
    `/卡 设 <字段> <值>` 可改，`/卡 详情 人物信息` 查看全文；
  - **资源字段**：`hit_dice_used`（短休已用生命骰）与 `inspiration`（激励 0/1），
    卡面新增「资源：生命骰 D6×4（短休已用 0/4）　激励 1/1」行——生命骰面值
    由知识库按职业派生（多职业去重），数量 = 总等级；
  - **已知法术**：`spells`（环阶 key → 法术名列表，key 归一为「戏法」+「1」..「9」，
    法术名保留「（来源）」标注）。卡面折叠为环阶统计「已知法术：戏法 5 个　
    1环 7 个　2环 2 个」，`/卡 详情 法术` 看全文；`/卡 设 法术
    「戏法:火焰箭,光亮术　1环:护盾术」` 整体覆盖。
- **先攻字段 + `/ri` 联动**：`initiative` 双层模型（base 由规则引擎按敏捷修正
  重算，bonus 为房规额外加值如警觉 +5）；卡面战斗核心行新增「先攻 +3」；
  `/ri` 无参数时若玩家有活跃角色卡，自动用卡上先攻掷 d20+调整值并标注
  「（角色卡：xxx 先攻 +3）」。
- **文本卡导入解析补强**（card_import）：
  - 「人物姓名：」「熟练项：」「人物描述：」键识别（熟练项五个类别独立行；
    人物描述 → 生平字段）；
  - 「子职：」「等级：」独立行作用于最近一个职业条目（兼容「职业：术士 /
    子职：狂野术法 / 等级：4」写法）；
  - 「生命骰/短休已用生命骰/激励/先攻」行解析（先攻与生命骰由规则引擎重算，
    导入不落值）；
  - 「已知法术：」整段与「戏法/一环/1环」独立环阶行两种形态；
  - 「护甲：无」等占位词过滤（不再误入防具熟练或装备槽）；
  - 「职业特性/种族特性/熟练加值」行排除误匹配（由知识库自动带出）。
- **技能别名**：「游说 → 说服」「特技 → 体操」（民间/3R 译名，显示仍用官方名）。

## [0.29.0] - 2026-08-08

### 新增

- **`/查职业` 本职特性细化**：此前 `/查职业 <职业> [子职]` 只输出职业本身
  特性的**名字总表**（如「1级：施法」），完整正文只在子职侧输出。现在
  第二参数支持「特性」关键词（命令 / 自定义前缀 / LLM 工具三入口同步）：
  - `/查职业 <职业> 特性` → 输出该职业**全部本职特性的完整正文**
    （按版本分组，`◆ N级 名称：` 格式与子职一致）；
  - `/查职业 <职业> 特性 <特性名>` → 只输出**单个特性**的完整正文
    （跨版本，如 `/查职业 奇械师 特性 施法` 同时给出 2014/2024 两版）；
  - 默认（不带「特性」关键词）输出保持精简：特性名总表 + 子职候选/子职全文
    （现状不变），末尾提示「查职业 <职业> 特性」可细化；
  - `class_features()` 增加 `feature` 参数（`"*"`=全部，具体名=单个，
    空串归一为 `"*"`）；`query_dnd_knowledge` 工具 32 → 33 参数
    （docstring 与 schema 回归测试同步）。

## [0.28.1] - 2026-08-08

### 修复

- **LLM 不再「扮演/编造」而不调工具**：在 AstrBot（尤其 v4.5+ 新 agent 体系，
  如 v4.27.x）中，插件工具虽已注入 LLM，但检定、车卡这类开放式任务上模型
  可能直接用自然语言扮演/编造，docstring 里的守则因工具未被调用而永远不会被
  看到。新增 `@filter.on_llm_request` 钩子，在每个 LLM 请求的 system_prompt
  末尾注入压缩版「跑团助手·工具守则」（**全部 7 个工具**按场景一句一条 +
  破坏性操作约束），让模型在生成回复前即知悉必须走工具：
  - 掷骰/检定 → `roll_dice`；规则数据（法术/怪物/物品等）→ `query_dnd_knowledge`；
  - 战斗先攻入列与推进 → `manage_initiative`；战利品与消耗 → `manage_inventory`；
  - 角色卡查看/修改/升级 → `manage_character`；车卡建卡 → `guide_chargen`
    逐步落库（禁止直接输出卡文本，已有文本卡引导 `/车卡 导入`）；
  - 买卖 → `manage_shop`；清空/删除/移除/取消等**破坏性操作不主动执行**，
    等玩家明确要求。
  守则带可检索标记、防重复追加、异常静默容错（不影响任何 LLM 流程），
  与 AstrBot 生态「工具触发引导」标准做法一致。

## [0.28.0] - 2026-08-08

### 新增

- **角色卡「语言」字段**：一个角色可登记多门语言（如「通用语、精灵语、龙语」）。
  自由文本、无词表校验，仅清洗去重限条数，旧卡零迁移（缺失即空）。
  - `/卡 设 语言 <通用语,精灵语>`：逗号/空格/顿号分隔，整体覆盖多门语言；
  - 卡面摘要新增「语言：…」行（排序展示）；
  - `/卡 详情 语言` 查看全部语言；
  - 文本卡导入支持「语言：…」独立行（与插件自身输出 round-trip）；
  - LLM 工具 `manage_character` 的 set 动作支持 `languages` 字段。

## [0.27.1] - 2026-08-08

### 修复

- **`/卡 设 背景` 不再覆盖生平**：「背景」此前被当作「生平/背景故事」的别名
  （v0.22.2 引入，当时 `/卡 设 背景` 报未知字段），导致真正的角色背景字段
  （`background`，如「侍僧」「士兵」）无法通过命令设置，输入还会误写入生平。
  现在「背景」归位到 `background` 字段，「背景故事/生平」仍为 `backstory` 别名；
  `update_fields` 新增 `background` 支持（≤40 字）；`/卡 详情 背景` 查看背景字段。
- 帮助文本与 README 同步更新（字段列表加入「背景」）。

## [0.27.0] - 2026-08-08

### 新增

- **法术一句话概要**：`/筛法术` 列表、`/查法术` 详情、搜索候选均展示 AI 生成的
  一句话能力概括（如「火球术 → 爆裂火球灼烧大范围区域，造成高额火焰伤害。」），
  法术 554 条（2014 补充书 + 2024 双书，reprintedAs 跳转后全部条目）全覆盖，
  存储于 `spells` 侧表新增的 `summary` 列（schema v7）。
- **法术语义标签反查**（`spell_keyword` facet，共 1119 条标签）：按需求找法术，
  如 `/筛法术 控场 3环`、`/筛法术 标签 治疗`、`/筛法术 伤害 减益`、`/筛法术 召唤`。
  - 标签体系：受控词表（`kb_enums.SPELL_KEYWORD_TAGS`，17 类：控场/伤害/治疗/增益/
    减益/召唤/位移/防护/侦查/潜行/社交/探索/幻术/即死/造物/战斗辅助/施法辅助）+
    少量自由词；与既有 `dmg_dealt`（伤害类型）/`condition_inflict`（状态）facet
    互补，不重复打标。
  - 裸词自动消歧：`resolve_spell_free_term` 查值集（spell_keyword 优先 → 形状/目标/
    成分/伤害/状态回退）；前缀词 `标签`/`关键字`/`关键词` 显式指定（避免与学派如
    「防护」混淆）；别名归一（`控制`→`控场`、`回血`→`治疗`、`瞬移`→`传送`、
    `防御`→`防护`…）。
- **LLM 法术检索**：`query_dnd_knowledge` 新增 `spell_keywords` 参数（32 参数），
  filter 动作 + `kind=法术` 时逗号分隔多标签 AND、别名归一，返回条目自带概要——
  LLM 结合角色卡与团情给出法术选取建议，如「3 环控场法术」→
  `filter + kind=法术 + level=3 + spell_keywords="控场"`。

### 变更

- 知识库 schema v6 → v7：`spells` 表新增 `summary` 列；`entry_tags` 新 facet
  `spell_keyword`；KB_SCHEMA_VERSION=7。构建期合并 `kb_patches/spell_enrich.json`
  （AI 逐条生成的概要+关键字，554 条全覆盖；键 (name,source,edition) 与 entries 对齐）。
- 数据源完整性核验：5etools-cn 法术源 936 条 − reprintedAs 跳转 382 条
  （PHB 361 全跳 XPHB、TCE/XGE 各 10、FTD 1）= 库内 554 条，无遗漏。
- `kb.filter` 对法术带出概要（`spell_summary`）；`resolve_spell_free_term` 裸词消歧；
  `format_entry`/`format_filter_result`/`format_hits` 法术优先用 AI 概要。
- 法术词表外自由词构建期统计告警（便于后续收敛补词表）。
- 测试 +3 → 846（spell_enrich 合并、/筛法术 标签反查与多标签 AND/环级组合、概要展示、
  llm_tool 法术筛选、schema v7；llm_tool schema 参数 31 → 32）。

## [0.26.1] - 2026-08-08

### 修复

- **专长详情显示属性提升与类型**：`/查专长` 头部展示「类型：通用；属性提升：力量、敏捷」
  （entry_tags 的 ability_increase/feat_type 汇总，仅 detail 查询带出；
  LLM 工具 detail 同步受益）。
- **2024 规则源误判为 2014**：ABH（阿斯代伦的饥渴之书）/EFA（Eberron: Forge of the
  Artificer）/FRHoF（Forgotten Realms: Heroes of Faerûn）/LFL（Lorwyn: First Light）/
  RHW（Ravenloft: The Horrors Within）为 2024 年 WotC 规则书，专长/背景原被标 2014
  （专长 2024 版 77 → 168 条），已补入 `EDITION_2024_SOURCES`。
  - 修复 upper 归一坑：官方缩写 `FRHoF` 含小写 o（`.upper()` 后变 FRHOF 失配），
    `edition_of_source` 改用 `_EDITION_2024_UPPER` 全大写比较；
  - 构建期新增校验：带 category 的专长未落 2024 时告警（防新书再漏判）；
  - `kb_patches/feat_enrich.json` 中 91 条专长 edition 同步 2014 → 2024。
- 测试 +2 → 843（edition_of_source 2024 源判定、/查专长 类型与属性提升展示）。

## [0.26.0] - 2026-08-08

### 新增

- **专长一句话概要**：`/筛专长` 列表、`/查专长` 详情、搜索候选均展示 AI 生成的
  一句话能力概括（如「巨武器大师 → 给使用重型武器提供更多伤害」），专长 276 条
  （2014/2024 双版本分别生成）全覆盖，存储于 `feats` 侧表（schema v6）。
- **专长能力标签反查**（`feat_keyword` facet，共 96 种标签 / 998 条）：按需求找专长，
  如近战角色找输出 → `/筛专长 近战 伤害`、提升机动 → `/筛专长 机动`、远程 → `/筛专长 远程`。
  - 标签体系：受控词表（`kb_enums.FEAT_KEYWORD_TAGS`，10 类 91 词：攻击方式/战斗输出/
    动作能力/防御生存/机动/控场/施法/技能熟练/探索冒险/特殊机制）+ 少量自由词（龙纹/
    光耀/弹药/毒素…）；武器属性沿用 WEAPON_PROPERTY canonical、技能沿用角色卡技能名。
  - 裸词自动消歧：语义标签优先，回退先决种族/特性/专长名；前缀词 `标签`/`关键字`/
    `关键词` 显式指定；别名归一（`重武器`→`重型`、`射击`→`远程`、`口才`→`说服`…）。
- **LLM 专长选取建议**：`query_dnd_knowledge` 新增 `feat_type`/`feat_keywords` 参数，
  filter 动作打开 `kind=专长` 支持（多标签逗号分隔 AND），返回条目自带概要——LLM 结合
  角色卡与团情（对话中获取）给出建议，如「5 级战士想提升近战输出」→
  `filter + kind=专长 + feat_keywords="近战,伤害"`。

### 变更

- 知识库 schema v5 → v6：新增 `feats(entry_id PK, summary)` 侧表；`entry_tags` 新 facet
  `feat_keyword`；KB_SCHEMA_VERSION=6。构建期合并 `kb_patches/feat_enrich.json`
  （AI 逐条生成的概要+关键字，276 条全覆盖；键 (name,source,edition) 与 entries 对齐）。
- `kb.filter` 对专长带出概要（`feat_summary`）；`resolve_feat_free_term` 消歧顺序改为
  feat_keyword 优先 → 先决种族/特性/专长；`format_entry`/`format_filter_result`/
  `format_hits` 专长优先用 AI 概要。
- 专长词表外自由词构建期统计告警（便于后续收敛补词表）。
- 测试 +4 → 841（feat_enrich 合并、/筛专长 标签反查与多标签 AND、概要展示、
  llm_tool 专长筛选；schema 参数 29 → 31；fixture 富化 6 条专长打标）。

## [0.25.0] - 2026-08-08

### 新增

- **`/筛专长` 专长反向查询**（别名 `ffilter`/`专长筛`）：三维反查，可组合条件：
  - **专长类型**：通用/起源/战斗风格/传奇恩惠/黑暗赠礼/龙纹（`/筛专长 战斗风格`）；
  - **属性值提升**：按属性名反查（`/筛专长 力量`），choose 二选一/多选一展开；
  - **先决条件反查**：种族（具体到名字，`/筛专长 半身人`）、属性门槛
    （`/筛专长 敏捷13`、`/筛专长 敏捷 13`、`/筛专长 前置属性 力量13`）、
    前置特性（`/筛专长 特性 战斗风格`）、前置专长（具体到名字，
    `/筛专长 巨人打击` 命中「云巨人之诡诈」等，含去括号基础名容错）。
  - 裸词自动消歧（类型 → 属性提升 → 先决种族/特性/专长，查值集判定）；
    前缀词 `类型/提升/属性/前置种族/前置属性/前置专长/前置特性` 指定精确维度。
- **构建期专长标签**：`_feat_tags` 产出 6 个 entry_tags facet（feat_type /
  ability_increase / prereq_race / prereq_ability / prereq_feat /
  prereq_feature），与既有筛怪/筛法术/筛物品共用 entry_tags 反查通道。

### 变更

- `kb.filter` 放行 `kind="feat"`（无侧表，仅按标签过滤）；新增
  `resolve_feat_free_term`（裸词维度判定）；kb_enums 新增 `ABILITY_CN`/
  `FEAT_CATEGORY_CN`/`resolve_ability`/`resolve_feat_type`。
- 知识库全量重建：专长 168 条类型标签、160 条属性提升标签、72 条先决条件标签。
- 测试 +4 → 837（构建标签、filter(feat)、裸词判定、/筛专长 命令；fixture 专长
  9 → 9 条含类型/提升/前置专长·种族·属性·特性全覆盖）。

## [0.24.1] - 2026-08-08

### 修复

- **专长前置条件中文化**：`/查专长` 不再把 `前置条件：[{'level': 4, ...}]`
  的原始 Python 字面量直接甩给玩家——构建期新增 prerequisite 结构化解析
  （`build_kb._feat_prereq_cn`），覆盖 5etools 全部 25 种形态：等级/属性
  门槛/护甲·武器·武器组熟练/前置专长/特性/种族/背景/战役/施法能力/施法特性/
  龙纹类别/其他文本等；数组元素间 OR（「或」）、单元素内 AND（「且」），
  共享等级自动提为公共前缀（「等级 4+，且 敏捷 13」）。此前 192/226 条
  （85%）专长 body 首段为英文 JSON 泄露。
- **专长 2014/2024 版本并存**：专长加入 reprintedAs 跳转豁免（与状态/种族
  一致）——2014 PHB 的 39 条专长（幸运/巨武器大师/哨兵/神射手等）此前因
  标注再版 XPHB 被构建期整条跳过，2014 团查不到原版规则；现在两版并存，
  `/查专长 幸运` 一次返回 2014+2024 两行。专长库 226 → 276 条（PHB 2014
  3 → 42、TCE 5 → 15、ERLW 1 → 2）。

### 变更

- 知识库全量重建（data_version 884d65a，专长前置条件与版本覆盖更新）。
- 测试 +2 → 833（前置条件中文化 5 断言、专长双版本并存；fixture 专长
  1 → 8，覆盖 proficiency/ability/feat/race/spellcasting/reprintedAs 形态）。

## [0.24.0] - 2026-08-08

### 新增

- **`/卡 降级 [职业名]`**（别名 `leveldown`/`降`/`down`）：指定职业 -1 级
  （缺省主职业），等级下限 1，战斗字段整卡自动重算——与 `/卡 升级` 对称，
  手滑升错可直接降回；重算失败不阻断（同升级）。

### 修复

- **子种族名不再被当成"未知种族"**：`kb.race_speed` / `kb.race_features`
  增加名称回退——精确名查不到时，若输入以某库内种族名结尾（如「银龙龙裔」
  →「龙裔」、「山丘矮人」→「矮人」）则按基础种族取速度与特性。此前用户
  手动填「银龙龙裔」这类 5etools 子种族名时速度与种族特性全部缺失；
  现在 2014 银龙龙裔 → 速度 30 尺 + 特性[龙族血统/吐息武器/伤害抗性]。

### 变更

- 帮助文案同步：/帮助 角色卡、`/卡` docstring、模块 docstring、未知子命令
  帮助、README 命令表与示例，均补充 `/卡 降级`。
- 测试 +10 → 831（level_down 单测 5 例、`/卡 降级` 命令 3 例、子种族回退
  4 例——fixture「山丘矮人」→矮人、无基础名不回退、精确优先）。

## [0.23.0] - 2026-08-08

### 新增

- **角色卡速度字段**：`CharacterSheet.speed`（双层 `LayeredStat`，base+bonus
  与 HP/AC 一致）。`recalc_base` 按卡上种族自动重算 base（`kb.race_speed`，
  取 races.speed_walk，edition 匹配；kb 不可用时保持原 base 不误删），车卡
  落库/文本导入/升级/装备变更全路径自动填充；`/卡 设 速度 <数字>` 写房规
  bonus（别名 `速度`/`speed`/`移动速度`）。
- **卡面显示种族特性**：`format_sheet` 新增「种族特性」行——`kb.race_features`
  从种族条目正文提取「特性名：」段落（过滤 年龄/体型/速度/语言 等基础描述
  键并去重），折叠前 8 条 + 「…等 N 项」；`/卡 详情 特性` 输出种族+职业
  特性全文。
- **`kb.race_speed(name, edition)` / `kb.race_features(name, edition)`**：
  知识库新增两个种族查询（races.speed_walk / body 段落名提取）。

### 变更

- **卡面布局按 DnD 惯例重排**：名字 → HP/AC/速度/被动察觉（战斗核心前置）
  → 职业 → 种族/背景/阵营 → 属性值 → 熟练/专精/专长 → 职业特性/种族特性
  → 法术位/攻击 → 装备 → 生平摘要。速度显示为 `速度 30尺`（含 bonus 时
  `30+5尺`）。
- **`/卡 专长` 用法提示补充整体替换方式**：`/卡 设 专长 <完整列表>`
  （如「巨武器大师,幸运」）可整体覆盖错误条目，不必逐个 `-` 删除。
- 帮助文案同步：/卡 设 字段列表加「速度」、/卡 详情 字段清单改为
  「特性（种族+职业）」、模块 docstring。
- 测试 +14 → 821（速度字段序列化/布局顺序/种族特性行、recalc 速度 5 例、
  kb race_speed/race_features 真实 fixture、/卡 设 速度 与 /卡 详情 特性）。
  fixture 迷你库矮人 entries 补一段具名段落供提取测试。

## [0.22.2] - 2026-08-08

### 新增

- **`/卡 详情 [字段]` 子命令**（别名 `detail`/`完整`）：查看卡面上被折叠的
  完整字段——`生平`（背景故事全文）、`专长`、`职业特性`（按职业等级过滤的
  全量列表）、`攻击`、`熟练`、`装备`；无参数时列出可查看字段清单。
- **`/卡 设` 新增「背景」「背景故事」别名**（映射到 `backstory` 生平字段）：
  此前只认「生平」，用户写「背景」会报「未知字段」（长背景故事写不进去）。

### 变更

- **`format_sheet` 长字段折叠（防整卡超长被平台截断）**：职业特性最多显示
  前 8 条、专长前 6 条、攻击前 8 项，超出部分以「…等 N 项」收尾并提示用
  `/卡 详情` 查看全文；属性/熟练/战斗字段/装备/生平摘要（50 字）不受影响。
  目标整卡 ≤800 字符，规避 QQ 等平台消息超长截断导致后半卡不可见。
- 帮助文案同步：/卡 设 用法、未知字段提示、/帮助 角色卡、模块 docstring
  均补充「背景/背景故事」别名与 `/卡 详情` 用法。
- 测试 +6（特性/专长/攻击折叠、阈值内不变、背景别名、详情命令），全量
  807 例。

## [0.22.1] - 2026-08-08

### 修复

- **知识库无独立条目的魔法武器不再丢失攻击条目（关键修复）**：`_calc_attacks`
  对 `item_combat` 查不到的装备名一律跳过，导致装备「警戒武器巨锤」「+1 巨锤」
  「巨人杀手巨剑」这类库中无条目的魔法武器变体（5etools 只有「警戒武器」书
  条目/战利品表，具名变体是搜索索引派生的）时**不生成任何攻击条目**——
  表现为 `/r 攻击` 报「卡上无攻击条目」（此缺陷自 v0.18 规则引擎起即存在，
  用户使用 v0.22.0 攻击联动时暴露）。修复：
  - `_calc_attacks` 对 `item_combat` 查不到的武器先按 `_resolve_base_weapon`
    解析基础武器名（词表 / kb base_item / 词表后缀，如「警戒武器巨锤」→
    「巨锤」、「+1 巨锤」→「巨锤」）复用其战斗属性（灵巧/远程/投掷判定），
    条目名保持原装备名；重算报告提示「按基础武器『巨锤』计算攻击属性」；
  - 仍解析不出（原创武器/火器等词表外）→ 回退「近战力量修正+熟练」生成
    条目并警告（沿用"词表外回退"哲学，细则交 DM）；
  - **行为不变项**：`item_combat` 有记录但 dmg1 为空（护甲/盾牌/杂物）仍
    跳过不生成条目（回归保护）。
- 测试 +4（具名魔法武器→基础武器近战/灵巧两路径、原创武器回退、非武器跳过
  回归），全量 801 例；真实知识库端到端复现用户卡（野蛮人 4 级 × 警戒武器
  巨锤 → 攻击加值 6 = 力修 4 + 熟练 2）通过。

> ⚠️ **部署后旧卡需重新重算**：修复的是 `recalc_base` 生成逻辑，已存在的
> 卡 `attack_bonuses` 仍为空，执行一次 `/卡 升级`（或改任意装备槽）即生成。

## [0.22.0] - 2026-08-08

### 新增

- **角色卡攻击检定联动（/r 攻击）**：`/r 攻击` 现在直接用角色卡 `attack_bonuses`
  的攻击加值掷攻击检定（`1d20+加值`），与属性/技能/豁免联动体验一致：
  - 无参数默认**主手武器**（`equipment.main_hand`，规则引擎算出的武器攻击加值）；
  - `/r 攻击 <武器名>` 按名字匹配（精确→前缀→包含，如 `/r 攻击 长剑`、
    `/r 攻击 长弓 15`），武器名与攻击表键不一致（如「巨锤」vs「+1 巨锤」）
    自动兜底匹配；
  - `/r 攻击 列表` 枚举卡上全部攻击选项（武器 + 「{职业}法术攻击」+ 手动条目）；
  - 尾数纯数字视为 DC（`/r 攻击 15` = 主手 + DC15），输出成功/失败判定；
  - 主手为空/未知名/无攻击条目 → 友好提示（未装备、相近候选、如何添加）；
  - LLM 工具 `roll_dice_tool` 与自定义前缀路由（`.r 攻击`）自动生效；
  - 提示类输出（列表/未找到）不写入 `/rh` 投掷历史（历史短路）。
- 攻击别名 token：`攻击` / `attack` / `atk`，仅在首 token 整词命中时触发，
  紧凑写法（`d20攻击` 等）不受影响。

### 变更

- `resolve_roll_alias` 返回的 kind 扩为 `{ability, skill, save, attack}`。
- `CharacterSheet` 新增只读方法 `list_attacks` / `main_hand_attack` /
  `resolve_attack`（不重算，直接读规则引擎已维护的攻击加值）。
- 帮助文本：/帮助 卡片段与骰子段、/r 指令 docstring 追加攻击示例。
- 测试 +25（别名识别不误伤、三个攻击方法、/r 攻击 全语义、历史短路回归），
  全量 797 例。

## [0.21.1] - 2026-08-08

### 修复

- **武器熟练判定支持魔法武器（关键修复）**：v0.21.0 的 `_weapon_proficient`
  对词表外武器一律「回退=熟练」，导致**只熟简易武器的角色拿军用魔法武器
  也会错误加上熟练加值**（如武僧拿「雷神之锤」，base_item=巨锤属军用）。
  现升级为「先解析基础武器名、再按类别严格判定」：
  - `kb.py` 新增只读查询 `item_base_item(name)`——具名魔法武器在
    `entry_tags.base_item` facet（217 条：雷神之锤→巨锤、阳炎剑→长剑、
    矮人飞锤→战锤、黑刃→巨剑等，2014/2024 均有）；
  - `_weapon_proficient(sheet, name, kb)` 判定链：空熟练项→熟练（旧卡兼容）
    → 精确命中武器名→熟练 → 基础名解析（本体在词表 / kb base_item /
    词表后缀如「+1 巨锤」→巨锤）→ 按简易/军用类别严格命中判定；
  - 仍解析不出类别（火器、原创武器）→ 回退=熟练（细则交 DM，行为不变）。
  - 行为平滑：未维护武器熟练的旧卡完全不受影响；只有显式维护熟练项后
    魔法武器判定才变严谨。
- 测试 +4（真实库矩阵验证：雷神之锤/阳炎剑/+1 巨锤/+1 短棒/词表外回退/
  无 kb 回退），全量 772 例。

## [0.21.0] - 2026-08-08

### 新增

- **技能专精（/卡 专精）**：卡片新增 `skill_expertise` 字段（独立于技能熟练，
  旧数据零迁移）。专精技能检定获得双倍熟练加值，/r 标签显示「专精+X」；
  对尚未熟练的技能加专精会给出提示（不阻断）。
- **吟游诗人万事通**：卡上有「吟游诗人」≥2 级自动生效——纯属性检定与
  无熟练的技能检定获得 ⌊熟练加值/2⌋（/r 标签「万事通+X」）；豁免与已熟练
  技能不受影响；被动感知自动联动（Sage Advice 规则）。等级由兼职列表动态
  计算，升级/兼职无需额外操作。
- **工具/武器/防具熟练项（/卡 熟练 工具|武器|防具）**：三类集合字段 +
  自由文本增删。武器熟练影响攻击加值：装备武器未熟练时 `_calc_attacks`
  不加熟练加值并在重算报告中提示（内置 PHB/XPHB 简易/军用武器中文词表，
  词表外武器与未维护武器熟练的旧卡回退为熟练，规则细则交 DM）。
- **职业特性与专长列表**：`format_sheet` 传知识库后追加「职业特性」段——
  按卡上各职业等级过滤 `kb.class_features` 只列名字（全卡截断 20 条）；
  「专长」为手动维护列表（/卡 专长 +名 -名，保存时提示知识库是否收录）；
  角色卡展示新增「被动察觉」。
- **文本卡导入扩展（card_import）**：支持解析 专精/专长/工具·武器·防具熟练
  行，与 format_sheet 输出 round-trip；旧文本卡零迁移。

### 变更

- `/卡 熟练` 类别由 技能|豁免 扩为 技能|豁免|工具|武器|防具；武器类别变更后
  自动触发战斗字段重算。
- llm_tool `manage_character` 新增 `expertise` / `feat` action，`prof` 的
  field 支持 5 类；docstring 同步更新（test_llm_tool_schema 回归保护）。

## [0.20.2] - 2026-08-08

### 修复

- **知识库 `_copy` 递归合并 + `_mod` 应用（关键修复）**：`build_kb._apply_copy`
  原先只做一层浅合并且从不应用 `_mod`，导致：
  - 二层 `_copy` 链（如 EGW 遗物「升华→觉醒→休眠」）合并后无正文被跳过，
    **漏收 20 条**（物品 16：碎镜之锋/达诺斯护目镜/无限法典/三祷之坠等升华形态；
    怪物 3：阿利格尔/金耶尔·德鲁吉尔/碎颅巨人；背景 1：间谍·奥根真知社）；
  - **约 970 条已入库条目正文残缺**：`_mod` 变体修改器从未应用——怪物变体
    动作区是基础版（如「阿弗纳斯的大公扎瑞尔」缺链枷/玛塔洛托克，战斗数据
    不可信）、进阶物品正文缺词条（碎镜之锋「觉醒」正文与「休眠」完全相同）。
  - 现按 5etools 官方 copyApplier 语义（js/utils.js）实现：递归合并 `_copy`
    链（深拷贝防污染父条目）+ `_mod` 全模式（insertArr 负索引/appendArr/
    prependArr/replaceArr/replaceOrAppendArr/appendIfNotExistsArr/removeArr/
    renameArr/replaceTxt 含 `{@tag}` 跳过与 tagInsensitive/replaceName/
    appendStr；`*` 键应用到怪物 12 字段、`_` 键无 prop 特殊处理）。
  - 重建 `kb_data/dnd_kb.db`：item 1853→1871（+16 升华 +2 补丁）、monster
    3738→3741（+3）、background 147→148（+1）。

### 新增

- **数据补丁通道（`kb_patches/`）**：5e.tools 上游未收录条目的补充机制。
  - `scripts/fetch_chm_patch.py`：从「5E 不全书」CHM 站
    （5echm.kagangtuya.top）抓取缺失条目，生成 `kb_patches/{kind}.json`；
  - `build_kb.py` 构建时自动合并 `kb_patches/` 补丁（测试用
    `patch_root` 参数隔离）；
  - 首批补丁：**警戒武器**（Weapon of Warning）DMG 2014 + XDMG 2024 双版本
    ——官方书存在（DMG p.213 / XDMG p.324）但 5e.tools 英文/中文站均未收录，
    此前 `/查物品 警戒武器` 查不到；现中英文名均可查。

### 其他

- 全量测试 719 例通过（新增 3 例：二层 _copy 链递归入库、_mod replaceArr、
  _mod insertArr/appendArr/replaceTxt；fixture 增补「遗物之刃」三形态模拟
  升华链，kb_patches 补丁在测试中经 `patch_root` 隔离）。

## [0.20.1] - 2026-08-08

### 修复

- **AstrBot v4.5+ llm_tool 事件注入兼容（关键修复）**：新版 agent 体系调用
  插件工具时，注入的第一个参数（event）从 `AstrMessageEvent` 变为
  `ContextWrapper`（真正的事件在 `.context.event`），导致所有 7 个 LLM 工具
  （roll_dice / manage_initiative / manage_inventory / manage_shop /
  manage_character / guide_chargen / query_dnd_knowledge）在 v4.5+ 下调用
  即报 `'ContextWrapper' object has no attribute 'unified_msg_origin'`。
  新增 `_resolve_event` 统一解包两种形态，无法识别时返回友好错误而非崩溃。

### 新增

- **manage_shop 工具 list 支持翻页**：新增 `page` 参数（每页 30 条，越界自动
  夹取到首/末页）；商品多于 30 条时返回末尾附「用本工具 page 参数再次调用」
  提示，LLM 可查看完整货架（如 page=2）。

### 其他

- 全量测试 716 例通过（新增 10 例：事件解包 4 态 + 4 个工具在 ContextWrapper
  注入下全链路执行 + 无法识别兜底 + LLM 工具翻页）。

## [0.20.0] - 2026-08-08

### 新增

- **商店系统（`/商店`）**：DM 配置商品列表、玩家自助买卖结算。
  - `/商店` 查看商品（库价/覆盖价、库存余量）；`买` 自动从背包扣款并找零；
    `卖` 只收在架商品、按「售价×回购系数」付款。
  - DM 子命令：`初始化`（一键上架 PHB/XPHB 非魔法物品，同名取 2024 价）、
    `上架`/`下架`、`设价`（金额或「自动」恢复库价）、`设库存`（数量或无限）、
    `回购率`（clamp 0–2）。管理操作需白名单/管理员权限。
  - LLM 工具 `manage_shop`（list/buy/sell），管理操作不暴露给 LLM。
  - 买卖跨商店+背包两把锁原子结算（固定锁序 Shop→Inventory），
    钱不够/库存不足整体不写入。
- **知识库 schema v5**：items 表新增 `value_cp`（价值，铜币）与 `weight_lb`
  （重量，磅）两列；`/查物品` 详情附「价值：X金Y银Z铜｜重量：N 磅」行。
  初始商店候选 = PHB/XPHB 非魔法有价物品 246 种（去重后）。
- **货币结算口径统一（money.py）**：背包价值字段统一为铜币，显示换算成
  「X金Y银Z铜」；货币条目（金币/银币/铜币）按面值自动计价，背包总价值
  = 折铜后的财富总额。
- **车卡起始金币自动发放**：车卡落库时按群规则发放起始金币（`/车卡规则
  起始金币 <自动|金额|骰式>`）——`自动`=按职业 goldAlternative 代骰（如
  战士 5d4×10 金币，2014 职业）；纯数字=全团固定金额（不代骰）；骰式=DM
  自定义随机财富代骰。发放写入个人背包「金币」条目（value 按面值）。
  2024 职业的起始财富内嵌在装备方案里需玩家选择，保持 LLM 引导、不自动
  发放。代骰/查询失败静默跳过、不阻断落库（失败时文案提示手动补发）。

### 变更

- 背包价值显示从裸数字改为币制（如 `v=50` 现显示「5银/件」）；旧手工
  数据按铜币口径重新解释（迁移见部署说明）。
- 交易失败文案细化（钱不够提示差额、售罄/无定价/非在架分别提示）。
- 车卡完成文案：2014 职业自动发放起始金币后不再提示 LLM 补金币（装备
  清单仍提示）；2024 职业提示不变。

### 其他

- 全量测试 703 例通过（新增商店买卖、货币结算、kb 库价、起始金币等用例）。

## [0.19.1] - 2026-08-08

### 变更

- **`/车卡规则` 紧凑写法容错**：设置项与值之间可省略空格——`版本2024`、
  `子职时机开`、`起始等级3`、`属性32buy`、`属性购点 池=32` 现在都会被正确
  拆分解析（此前 `版本2024` 会被整体当作未知设置项拒绝，提示
  「未知的设置项 '版本2024'」）。仅拆一层（值内的深层紧凑如
  `属性购点池=32` 不支持，请用标准语法）。
- `/车卡规则` 帮助文本补充紧凑写法示例；测试新增 7 例（解析器 6 例 +
  命令层 1 例）。全量 647 例通过。

## [0.19.0] - 2026-08-08

### 新增

- **文本角色卡导入**（`card_import.py`）：`/卡 导入 <卡文本>` 与
  `/车卡 导入 <卡文本>` 把玩家贴出的文本角色卡直接解析落库——兼容插件
  `format_sheet` 输出（round-trip）与 LLM 生成的宽松 key:value 格式；
  属性越界自动 clamp 1-30 并提示；战斗字段（HP/AC/法术位/攻击）一律忽略
  交由规则引擎重算；缺失字段给默认值并逐条 notes；有效字段不足时拒绝并
  给出格式示例。导入成功自动丢弃该玩家的车卡草稿（防残留），同名卡沿用
  覆盖语义。
- **防脱轨守则**：`guide_chargen` 工具 docstring 与每步返回新增
  【守则】提示——禁止在对话中输出完整角色卡文本，玩家要求直接出卡也必须
  继续调用工具；已有文本卡引导使用 `/车卡 导入`。`manage_character` 的
  show 无卡文案同步补导入引导；`/卡` `/车卡` 帮助与 `/帮助` 数据块补导入
  用法。
- **测试**：新增 `test_card_import.py`（14 例：round-trip 2014/2024、
  宽松格式、缺省默认、clamp、edition 识别、未知行忽略、无效输入拒绝）与
  `test_card_import_commands.py`（20 例：/卡 导入、/车卡 导入、
  custom_prefix 三入口、草稿清理、同名覆盖、守则断言）。全量 640 例通过。

## [0.18.0] - 2026-08-08

### 新增

- **全自动规则引擎**（`chargen_engine.py`）：输入角色卡（职业/等级/种族/背景/
  装备槽）自动计算战斗字段 **base 层**——HP（首职首级满骰 + 其余期望值 +
  体修）、法术位（full/half/third/artificer/pact + 兼职施法者合并折算）、
  AC（轻/中/重甲 + 盾牌 + 无甲防御取高）、攻击加值（装备槽武器：近战力修/
  灵巧取高/远程敏修；各施法职业「法术攻击」条目）。**只动 base 不动 bonus**，
  bonus 保留为房规调整层；玩家自建攻击条目整体保留。
- **无甲防御（PHB 三种）**：野蛮人 10+敏+体（可持盾）、武僧 10+敏+感、
  术士子职「龙族血脉」13+敏，与穿甲 AC 取最高。
- **升级命令** `/卡 升级 [职业名]`：指定职业 +1 级（缺省主职业；新职业追加
  兼职），整卡自动重算 base；`manage_character` 工具新增 `level_up` action。
- **装备槽联动**：`/卡 设 主手/副手/护甲` 后自动按物品战斗数据重算 AC/攻击
  （双手武器顶掉盾牌等冲突会警告）。
- **属性加值全自动**：车卡引导新增「加值选择」步（`ABILITY_BONUS`）——2014
  半精灵式 `choose`（选 N 个各 +1）与 2024 背景 `weighted`（+2/+1 或
  +1/+1/+1 二选一）由插件列出方案并硬校验，落库时自动叠加（clamp 1-30）；
  固定加值（2014 种族平铺）直接自动应用。替代 v0.17 的 LLM 引导+DM 复核。
- **知识库 schema v4**（`build_kb.py`）：新增 6 张规则引擎侧表——
  `class_combat`（生命骰/豁免/施法进度/施法属性）、`subclass_caster`
  （子职施法进度，奥法骑士/诡术师 1/3）、`class_starting_equipment`、
  `background_ability`/`race_ability`（加值 payload）、`item_combat`
  （护甲/武器战斗字段）；kb.py 新增 5 个专表查询方法。法术位表按
  casterProgression 硬编码标准表（full/half/third/artificer/pact；
  XPHB 圣武士/游侠为 artificer=向上取整，1 级即有施法）。
- **测试**：新增 `test_chargen_engine.py`（34 例：HP/法术位/AC/攻击/写回
  策略）、kb 侧表查询、加值选择步全流程、`level_up`、`/卡 升级` 命令与
  装备重算。全量 606 例通过。

### 变更

- 战斗字段语义：`base` 由规则引擎计算，`/卡 设 hp/ac/法术位N/攻击` 仍写
  **bonus**（房规调整），命令语义不变；`/卡 设` 装备槽后自动重算。
- 车卡流程：`ABILITY_ASSIGN` 后插入加值选择步（无自选加值时自动跳过）；
  落库时自动叠加种族/背景加值并重算战斗字段，不再需要 LLM 计算与 DM 复核。
- 帮助文本：角色卡组新增 `/卡 升级` 与「战斗字段自动计算」说明。

### 已知限制（v0.18）

- PHB 人类（2014）的 +1 全属性在 5etools-cn 源数据中无结构化字段
  （`ability: null`），未自动叠加，需玩家分配时自行计入。
- 邪术师在数据源中名为「魔契师」（cn2.0 译名），卡上职业名需用「魔契师」。
- 已知等级上限 20、单职业上限 20；升级重算依赖知识库 v4（旧库自动回退
  内置库，需重跑 build_kb.py 才有引擎数据）。
- 攻击条目按「生成集外整体保留」兜底，改名武器后旧条目会残留为手动条目。

## [0.17.0] - 2026-08-08

### 新增

- **角色卡**（`/卡`）：多卡 + 活跃切换（一玩家一会话可多张命名卡）。
  字段含属性值/职业（兼职列表）/种族/背景/阵营/熟练/战斗字段双层
  （base+bonus）/装备槽/生平/命名掷骰；`/r 力量`、`/r 察觉`、
  `/r 敏捷豁免` 自动查活跃卡拼 `1d20+修正`（含熟练加值，注明来源）。
- **LLM 引导车卡**（`/车卡` + `guide_chargen` 工具）：插件状态机 +
  LLM 工具返回驱动（【进度】【校验】【下一问】三段式，一次一问）。
  2014（种族→职业→背景）与 2024（职业→起源→属性）双路径；生平按
  XGE「出身→个人决定→人生经历」三段引导；草稿 KV 跨重启可续传，
  落库后自动成为活跃卡。
- **群开卡规则**（`/车卡规则`，DM/管理员设置，群聊写入需白名单权限）：
  规则版本、属性生成方式（参数化模板 + 内置别名 27buy/32buy/dnd5/
  标准数组，可自定义购点点数池/区间与骰式）、子职时机、起始等级。
  校验硬拒绝（购点超池/出区间/骰池多重集不符/标准数组不符 → 拒存并
  指向 DM 改规则）；掷骰法由插件代骰（明细入草稿+投掷历史）。
- **LLM 工具 ×2**：`manage_character`（卡 CRUD/设字段/熟练/命名掷骰）、
  `guide_chargen`（start/answer/status/cancel）。
- **知识库联动**：RACE/CLASS/BACKGROUND/ORIGIN 步经知识库精确命中
  校验，未命中返回候选重问；LLM 引导时提示用 `/查种族|职业|背景` 答疑。
- **测试**：新增 `test_character.py`（序列化容错/多卡 KV/别名）、
  `test_chargen.py`（规则/校验器/双路径状态机全流转）、
  `test_character_commands.py`（三入口一致性/权限//r 联动）；schema
  回归新增两工具断言。全量 530 例通过。

### 变更

- `character.py` 从内存 LRU 存根升级为 KV 持久化（`character:*` 前缀）；
  旧存根无持久化数据，无需迁移。
- `/r 感知` 等纯别名输入的行为变更：无活跃卡时仍为解析报错（原行为），
  有活跃卡时自动查卡加修正。
- 帮助系统新增「角色卡」组（`/帮助 角色卡`）。

### 已知限制（v0.17）

- 战斗字段（HP/AC/法术位/攻击加值）手动填 bonus 层；HP/法术位当前值
  追踪与战斗资源管理归未来「战斗管理器」。
- 种族/背景属性加值（2014 种族、2024 背景）由 LLM 引导计算、DM 复核
  （分层校验）；v0.18 规则引擎数据管线到位后升级为全自动计算。

## [0.16.0] - 2026-08-07

### 新增
- **状态条目**：新增 `/查状态` 查询（kind=状态）。收录 `condition` + `status`
  共 35 条（目盲/魅惑/力竭… + 2024 浴血/专注等），2014/2024 双版本并存
  （`reprintedAs` 不跳转）；`/查询` 广搜与 LLM 工具 `kind=状态` 同步支持。
- **种族条目**：新增 `/查种族`（kind=种族，160 条，同名多版本）。详情头部
  展示【种族信息】体型/速度/黑暗视觉/生物类型/抗性/天生施法 + 条目全文。
- **种族六大维度反查**（`/筛种族` + LLM 工具 filter）：
  - **速度**：类型词独立 token（步行/攀爬/游泳/飞行/掘穴，缺省步行）；
    裸 `40尺` = 至少 40（`40尺以下` = 至多 40）；只走结构化 `speed` 字段
    （`true` = 等同步行速度），正文临时飞行（变身）不收录；
  - **体型**：`/筛种族 中型`（`["S","M"]` 多体型命中）；
  - **生物类型**：`/筛种族 类人生物/亡灵/妖精/构装/泥怪…`，有 `creatureTypes`
    按字段（骷髅=亡灵、流浆体=泥怪…），无字段默认类人生物；
  - **天生抗性**：`/筛种族 火焰` = 对火焰伤害有天生抗性（2024 结构化
    resist/immune/vulnerable 含 choose 变体 + 2014 正文「对X伤害具有抗性/
    免疫/易伤」文本双通道）；
  - **黑暗视觉**：`/筛种族 黑暗视觉` = 有无，`黑暗视觉 60尺` = 距离 ≥ 60；
  - **天生施法**：`/筛种族 迷踪步` 按法术名反查（2024 `additionalSpells`
    innate+known + 2014 正文 `{@spell}` 标签双通道，未知 token 兜底查法术库）。
- **LLM 工具升级**：`query_dnd_knowledge` 参数 22 → 29（新增 speed_type/
  speed_min/speed_max/size/creature_type/darkvision_min/innate_spell）；
  `kind=种族` 时 `damage_type` 语义为天生抗性。
- **schema v3**：新表 `races`（速度/黑暗视觉数值列）；`resolve_db_path`
  自动回退旧 v2 更新库，无需手动清理。
- `kb_data/dnd_kb.db` 已重建（schema v3，含状态/种族）。

## [0.15.0] - 2026-08-07

### 新增
- **物品稀有度反查（中文取值）**：`/筛物品` 与 LLM 工具 `query_dnd_knowledge`
  的 `filter` 现支持完整稀有度反查：
  - 六档标准稀有度：普通/非普通/珍稀/极珍稀/传说/神器；
  - **非魔法物品**：全部基础物品（`rarity=none`，`/筛物品 非魔法物品`）；
  - **魔法物品**：整体反查（`/筛物品 魔法物品`，排除非魔法基础物品）。
- **稀有度中文显示**：`/查物品` 详情与 `/筛物品` 列表的稀有度改为中文显示
  （普通/非普通/珍稀/极珍稀/传说/神器/非魔法物品/未知/未知稀有度/多种稀有度）；
  非魔法物品直接显示「非魔法物品」而非「稀有度：none」。
- **物品类型显示中文化**：详情【物品信息】中的类型码（如 `M|XPHB`）改为
  中文（「类型：武器」），复用 item_type 码表。
- `kb_data/dnd_kb.db` 已重建（物品正文稀有度/类型中文固化）。

## [0.14.0] - 2026-08-07

### 新增
- **物品类型反查**：
  - `/筛物品` 新增「物品大类」条件：武器/盾牌/重甲/中甲/轻甲/法器/乐器/工具/权杖/
    魔杖/戒指/药水/卷轴/坐骑/载具…（`/筛物品 武器`、`/筛物品 权杖`）。
  - `/筛物品 <基础物品名>` 列出全部以它为原型的魔法武器（`/筛物品 长剑` → 19 件
    长剑系魔法武器），可与其他条件组合（`/筛物品 长剑 暗蚀`）。
  - `/查物品 长剑` 在返回基础条目详情后，自动附加「以长剑为基础的魔法物品」列表。
  - 构建期提取：物品 `baseItem` 字段 → `entry_tags` 的 `base_item` facet；
    `type` 码（M/R/S/HA…）→ `item_type` facet（基础与魔法物品共用一套码表，
    实测两侧同码语义一致，如 S=盾牌）。
- **法术按学派反查**：`/筛法术 惑控`、`/筛法术 死灵` 直接列出该学派法术
  （此前已支持，本次补全测试与文档）；`/查询 <学派词>` 在名称无命中时
  兜底列出该学派全部法术（如 `/查询 惑控`）。
- **学派显示修复**：法术正文的「学派V」等单字母改为中文「学派塑能/学派死灵…」
  （新增 SCHOOL_CN_REV 反向映射，构建期渲染）。
- **LLM 工具升级**：`query_dnd_knowledge` 的 `filter` 动作新增 `base_item`
  与 `item_type` 参数（共 22 个参数）。
- **目标启发式修正**：法术「多体」判定从裸「每个」改为「每个/所有 + 生物类名词」，
  避免「在每个回合结束时」这类豁免时机描述把单体法术误判为多体。
- `kb_data/dnd_kb.db` 已重建（含 base_item/item_type 标签与学派中文正文）。

### 修复
- **`/查物品` 机翻标记 NameError**：`/查物品 <基础物品名>` 的附加列表中，机翻
  物品（is_machine=1）触发 `_MACHINE_FLAG` 引用报错（该常量为 kb.py 私有且
  main.py 未导入）。已通过 kb.py 公开别名 `MACHINE_FLAG` 修复，并新增
  机翻长剑系 fixture 条目做回归测试（此前 fixture 全为人工翻译，机翻分支
  从未被覆盖）。

## [0.13.0] - 2026-08-07

### 新增
- **特性反查（按属性筛条目）**：新增 `/筛怪`、`/筛法术`、`/筛物品` 三个指令，
  用自然条件反查条目，多条件空格分隔自动组合（AND）：
  - 怪物：伤害类型（`/筛怪 火焰 CR5以下`）、状态、环境、怪类、CR；
  - 法术：伤害类型、状态、成分（言语/姿势/材料）、专注、环级、距离
    （`30尺`/`30尺以上`/`触碰`/`自身`）、范围形状（球形/锥形/线形/柱形/立方）、
    目标（单体/多体/自我）、学派；
  - 物品：伤害类型（`/筛物品 暗蚀 灵巧`）、状态、武器属性（灵巧/重型/轻型/双手/
    投掷/两用…）、稀有度、需同调。
  - 未识别的条件会被忽略并在结果末尾提示（`未识别条件：XXX`）。
- **`/查询` 跨库广搜**：一次搜索法术/怪物/物品/专长/背景/职业全部类别，结果按
  类别分组展示；加 `-全文` 参数可额外搜索条目正文。
- **构建期结构化提取**：`scripts/build_kb.py` 从源数据提取特性标签入新表
  `entry_tags`（facet+value 通用模型，含 造成/抗性/免疫/易伤伤害、状态施加/
  状态免疫、环境、武器属性、法术成分/形状/目标），schema 升级到 v2：
  - 怪物动作区中文正则提取伤害类型（13 种 canonical 词 + 变体别名，
    如 黯蚀→暗蚀、冷冻→寒冷）；`{@condition}` 标签在清洗前提取状态；
    immune/resist/vulnerable/environment 结构化映射（含 dict 变体）；
  - 物品新增基础物品（items-base.json，230 条，含 101 件武器）入库；
    解析 dmgType 码与 weapon property 码、entries 附加伤害文本；
    `_copy` 派生条目浅合并基字段；`reprintedAs` 旧版条目跳过并重定向别名；
  - 法术新增 components / range_feet / range_type 列（专注/距离过滤的数据基础）。
- **LLM 工具升级**：`query_dnd_knowledge` 的 `filter` 动作新增 10 个特性参数
  （damage_type/condition/environment/weapon_property/components/concentration/
  shape/target/range_type/range_max），AI 可直接按「造成火焰伤害的怪物」等
  自然语言条件查询。
- **旧更新库自动回退**：`resolve_db_path` 增加 schema 版本检查——旧版
  `kb_update.db`（schema v1）不再屏蔽新版内置库，无需手动删除。
- `kb_data/dnd_kb.db` 已重建（schema v2，约 14MB，含标签索引）。

### 修复
- 修复构建脚本对正文中「X伤害」词汇的告警收集可能在个别文本上抛
  `KeyError` 导致整次构建失败的问题。

## [0.12.4] - 2026-08-07

### 新增
- **施法属性展示**：施法怪物的每个施法块现标注依赖的施法属性（如「施法：（施法属性：智力）」，来自 spellcasting.ability 字段，含六属性映射）。此前仅 headerEntries 文本偶有提及，能力字段完全未渲染。
- `kb_data/dnd_kb.db` 已重建。

## [0.12.3] - 2026-08-07

### 新增
- **怪物词条全面补全**（此前仅展示体型/AC/HP/速度/CR 与特性/动作等段落）：新增
  - 【属性】六项属性值与修正（如 力量17(+3)）
  - 【豁免】豁免熟练加值（36% 怪物有此字段）
  - 【技能】技能加值（54%，含 18 项技能中英映射，如 察觉/奥秘/洞悉）
  - 【免疫】/【抗性】/【易伤】伤害类型（含条件免疫，如"来自非魔法攻击"）
  - 【状态免疫】（魅惑/恐慌/麻痹等）
  - 【语言】、【感官】（含被动察觉）、【装备】（gear）、【环境】
  - 速度补全全部类型（步行/飞行/游泳/攀爬/掘地/悬浮）
- **施法怪物法术列表**：解析 spellcasting 结构化字段（1157 个施法怪物，26%），展示随意施展、戏法、各环法术位、每日次数等完整法术清单（如巫妖 0-9 环全部法术）。
- `kb_data/dnd_kb.db` 已重建。

### 修复
- **CR 字段多态解析**：部分怪物 cr 为 `{"cr":"21","xpLair":…}` 字典（此前显示乱码、CR 过滤失效），现支持 dict 形式。

## [0.12.2] - 2026-08-07

### 修复
- **升环伤害显示错误**：`{@scaledamage 10d4|4-9|2d4}`（基础伤害|等级区间|每环增量）此前清洗取第一段，导致「浓酸球」升环显示"增加 10d4"（正确为 2d4）、「火球术」升环显示"增加 8d6"（正确为 1d6）。scaledamage/scaledice 改为取第三段（每环增量）。
- **怪物 AC/HP 缺失**：源数据中 HP 为 `{"average":68,"formula":"8d10+24"}`、AC 为 `[17]`/`[{"ac":19}]` 等 dict/list 结构（全库 3480/3447 处），构建代码只接受 int/str 导致全部丢弃。现支持 dict/list 结构解析，怪物正文恢复「ACxx｜HPxx（骰式）」数据行。
- **2024 版怪物标签噪音**：补齐 `{@atkr m|15}`（近战/远程）、`{@actSave dex}`（敏捷豁免等）、`{@actSaveFail/Success}`（豁免失败/成功）映射，并折叠 `{@h}：` 造成的双冒号。
- `kb_data/dnd_kb.db` 已重建。

## [0.12.1] - 2026-08-06

### 修复
- **子职特性输出全文**：`/查职业` 的子职特性此前只显示首句摘要（如「明月激励」只显示"使你获得以下增益。"），现改为输出特性完整正文（含嵌套条目块）。
- **修复 list 块内容被丢弃**：5etools 的 `{"type":"list","items":[...]}` 块（职业数据中 92 处）此前构建时被整体丢弃，现逐项展开成行；`item` 类型块同步支持。
- `kb_data/dnd_kb.db` 已重建：背景条目 75 → 135（此前因正文渲染为空被跳过的条目恢复），职业特性正文普遍补全。

## [0.12.0] - 2026-08-06

### 新增
- **群内指令大全**：新增 `/帮助`（别名 `/菜单` `/menu` `/commands`）——群聊玩家可随时查看插件全部指令：无参数返回分组概览（知识库/先攻/背包/骰子/历史），`/帮助 <组名>` 返回该组详细语法（如 `/帮助 知识库`）。自定义触发前缀同步支持（`.帮助`）。

## [0.11.3] - 2026-08-06

### 变更
- `query_dnd_knowledge` 工具描述升级为强指令：玩家问及任何 D&D 5e 规则数据时**必须**调用本工具、禁止凭模型记忆回答（工具描述是 LLM 决定是否调用的关键输入，写强指令可显著提高调用率）。配置层面的配合步骤见部署说明「让 LLM 稳定调用工具」一节。

## [0.11.2] - 2026-08-06

### 修复
- **修复绝大多数子职查不到特性的问题**：5etools 的 `subclassFeature` 只带子职短名（`subclassShortName`，如「塑能」），而子职显示名是 `name`（如「塑能学派」）；构建时只存了短名，导致 214/322 个子职按显示名查不到（如「法师 塑能学派」「野蛮人 狂战士道途」）。现在构建时建立 短名→显示名 映射，`class_features` 表双存 `subclass_name`（显示名）与 `subclass_short`（短名），查询时两者皆可匹配，候选列表展示显示名。
- `kb_data/dnd_kb.db` 已重建（子职特性数据更新）。

## [0.11.1] - 2026-08-06

### 修复
- **修复 LLM 工具参数 schema 为空**：`query_dnd_knowledge`（及 `manage_initiative`、`manage_inventory`）的 docstring 未按 AstrBot 要求的 Google 风格 `Args:` 段书写，导致 AstrBot 注册工具时参数解析为空，LLM 拿到的工具没有任何参数定义（表现为「LLM 没法正确拼写查询语句」）。三个工具 docstring 全部改为标准 `Args:` 格式（参数名(类型): 描述），实测解析出完整参数列表。
- 新增 `tests/test_llm_tool_schema.py` 回归测试（4 用例）：复刻 AstrBot 的 docstring 解析逻辑，断言每个 `@filter.llm_tool` 函数的 docstring 参数与函数签名一致、类型合法（string/number/boolean）、描述非空，防止再次写坏格式。

## [0.11.0] - 2026-08-06

### 新增
- **DND 知识库（Knowledge Base）**：新增 `kb.py` 查询层、`kb_tags.py` 标签清洗、`kb_enums.py` 中文枚举映射与 `scripts/build_kb.py` 离线构建脚本；内置只读 SQLite（`kb_data/dnd_kb.db`，约 9.9MB），数据源自 5etools 中文站（cn2.0）结构化 JSON。
- **六类数据**：法术 936 / 怪物 3572 / 物品 2388 / 专长 276 / 背景 75 / 职业 30 + 子职 322（构建产物实测）。
- **查询指令**：`/查法术` `/查怪` `/查物品` `/查专长` `/查背景` `/查职业 <职业> [子职]` 与 `/kb version`；自定义触发前缀（`.查法术` 等）同步支持。
- **同名多版本**：同名法术/怪物/职业在 2014/2024 两版并存时全部列出并标注来源（`[PHB·2014]`、`[XPHB·2024]`）。
- **模糊搜索**：别名精确 → 名称 LIKE → 逐字缩短三级容错（如「火球木」→「火球」）。
- **结构化筛选**：怪物按类型+挑战等级、法术按环级+学派、物品按稀有度过滤（LLM 工具 action=filter）。
- **机翻标注**：与 5etools-cn 网站一致的判定规则（`translator` 缺失或非「不全书」→ ⚠️机翻）。
- **LLM 工具**：新增 `query_dnd_knowledge`（action: detail/search/filter/class_features/version），返回原文并固定附带「仅可依据其作答」零幻觉约束；查无结果明确返回「未找到」。
- **合规**：新增 `NOTICE`（CC BY-NC-SA 4.0 署名声明）、README「数据与许可」节、`docs/adr/0002` 与 `0003`、CONTEXT.md 知识库术语。
- 新增 pytest 用例（约 100 条）：kb_tags 标签清洗、构建产物 schema、查询层三档搜索/多版本/过滤、命令全链路、custom_prefix、LLM 工具各 action。

### 已知限制
- 职业-法术关联（某职业能学哪些法术）本期不提供：上游中文法术数据不带 `classes` 字段。
- `/kb update` 在线更新规划中；更新知识库 = 安装新版插件 zip。
- 机翻条目约占一半（怪物尤多），返回带 ⚠️ 标注，由 DM 甄别。

## [0.10.0] - 2026-08-06

### 变更
- **正式更名为「跑团助手」（astrbot_plugin_trpg_assistant）**：插件 ID、Python 包名、目录名、主类名（`TrpgAssistantPlugin`）、日志前缀（`[trpg_assistant]`）全部更新，与原 D&D 骰子插件彻底区分。
- **注意（数据隔离）**：AstrBot 按插件 ID 隔离 KV 存储，更名后旧插件（`astrbot_plugin_dnd_dice`）的先攻列表、投掷历史、背包数据**不会自动迁移**。需保留旧数据时，请先卸载旧插件前做好记录，或回滚旧版本导出后再迁移。
- MIT 许可保持不变：LICENSE 保留原作者版权声明，README 增加 fork 声明与上游链接。
- `metadata.yaml` 的 `repo` 字段保留上游地址（发布到自有仓库后请改为自己的 fork 地址）。

## [0.9.0] - 2026-08-06

### 新增
- **物品属性编辑**：新增 `/bag edit <名称> [w=单件重量|-] [v=单件价值|-] [note=备注|-]`，事后修改物品的重量/价值/备注，只改提供的字段、不涉及数量与名称；`w=-` / `v=-` / `note=-` 将对应属性恢复为「未设置」。
- `/bag party edit`：修改队伍背包物品属性（全员可用，与 put/take 同权；私聊不可用）。
- `manage_inventory` LLM 工具新增 `action="edit"`：weight/value 不传=不修改、`-1`=清除、`>=0`=覆盖；note 不传/空串=不修改、`"-"`=清除。
- 新增 pytest 用例（28 条），覆盖三态编辑语义、清除标记解析、命令全链路、队伍背包编辑与 LLM edit action。

## [0.8.0] - 2026-08-06

### 新增
- **背包管理（Inventory）**：新增 `inventory.py` 模块与 `/bag` 指令族，「个人背包 + 队伍背包」双模型：
  - `/bag`（可带 `list @某人`）：查看自己或他人的个人背包（他人只读），显示总重量/总价值（未设属性的物品按「至少」统计，追加 `+`）。
  - `/bag add <名称> <数量> [w=单件重量] [v=单件价值] [note=备注]`：放入物品；同名合并数量、提供的属性覆盖旧值；物品名含空格可用英文双引号包裹。
  - `/bag rm <名称> [数量]`：取出物品（数量缺省 1），归零自动删除条目。
  - `/bag party` / `/bag put` / `/bag take`：查看队伍背包、存入、取出；队伍背包会话内全员共享，私聊中不可用。
  - `/bag give @某人 <名称> [数量]`：玩家间赠送（目标从消息链 At 组件提取）。
  - `/bag clear` 清空自己的背包；`/bag party clear` 清空队伍背包（群聊需白名单/管理员权限，与 `/rh clear` 同规则）。
- 物品流转（put/take/give）在管理器单把锁内完成「源扣除 + 目标合并」原子写入，源数量不足时两侧均不变。
- 货币不作为独立钱包：金币等即为普通物品条目（见 `docs/adr/0001`）。
- 新增 `manage_inventory` LLM 函数工具（action: add/remove/list/put/take/clear），AI 主持人可自动管理背包；不暴露 give 与队伍背包清空。
- 自定义触发前缀（`.bag` 等）同步支持背包指令。
- 新增 `CONTEXT.md` 术语表与 `docs/adr/0001-inventory-dual-model-kv.md` 架构决策记录。
- 新增 pytest 用例（80+），覆盖序列化容错、合并语义、流转原子性、会话/玩家隔离、命令全链路、私聊边界、权限判定、自定义前缀路由与 LLM 工具。

## [0.7.1] - 2026-08-06

### 修复
- 修复 `/init del <名称>` 与 `/init 移除 <名称>` 等带参数子命令永不生效的问题：子命令匹配此前使用整段参数字符串（如 `"del 食人魔"`），永远匹配不上 `del` 分支，直接落入用法提示；现改为只匹配第一个 token，剩余部分作为目标名称。该问题同时影响自定义触发前缀路由（`.init del <名称>`）。

### 新增
- 新增命令级集成测试套件 `tests/test_integration_commands.py`（22 用例）：将 `TrpgAssistantPlugin` 真实实例化，以内存 KV 假 Star 与假消息事件模拟 AstrBot 消息管线，直接驱动 `ri_cmd` / `init_cmd` / `manage_initiative_tool`，覆盖指令参数接线、权限判定（群聊非管理员拒绝 / 管理员放行 / 私聊放行）、会话隔离与 LLM 工具调用。

## [0.7.0] - 2026-08-06

### 新增
- **先攻追踪（Initiative）**：新增 `initiative.py` 模块与 `/ri`、`/init` 指令族，按 DnD 5e 规则管理战斗先攻：
  - `/ri`（可带 `+调整值` / `固定值` / 名称）：掷 d20 入列、为具名单位代掷、固定值录入（DM 抄书值），三种录入方式全支持。
  - `/init`：查看先攻列表（降序排列，同值先报先动，▶ 标记当前行动者，显示轮数）。
  - `/init end`：推进回合，自动播报下一位行动者，绕回列表顶部时轮数 +1。
  - `/init del <名称>`：移除单位（倒下/离场），移除当前行动者时指针自动接力（含平手规则）；`/init clr`：清空本场战斗。
- 先攻列表按会话隔离、KV 持久化、不自动过期，跨周续团不丢状态；破坏性指令（del/clr）在群聊中走白名单/管理员权限（与 `/rh clear` 同规则），`/ri`、`/init`、`/init end` 全员开放。
- 新增 `manage_initiative` LLM 函数工具（action: roll/fixed/list/end/remove/clear），AI 主持人可自动管理战斗先攻。
- 自定义触发前缀（`.r`/`.init` 等）同步支持先攻指令。
- 先攻条目预留 `modifier` / `user_id` 字段，为角色卡系统落地后的自动取调整值做准备。
- 新增 pytest 用例（30+），覆盖参数解析三分支、排序与平手、回合推进与绕回轮数、移除指针校正、清空、会话隔离、脏数据容错与格式化输出。

## [0.6.2] - 2026-07-19

### 修复
- 修复穿透爆炸骰（`!p`）无法连锁的问题：追加骰值曾被钳制为最小 1，且爆炸链只能触发一次；现按未减 1 的原始值判定是否继续爆炸，追加骰值允许为 0。
- 修复复合爆炸（`!!`）在重骰之前执行导致重骰条件误判合并总值而非单骰原始值的问题：改为先重骰再合并爆炸链，语义与 `!`/`!p` 保持一致。
- 爆炸骰预算检查改为生成过程中逐颗增量强制，避免大骰池 × 大爆炸深度组合在掷完之后才发现超限；新增自定义爆炸条件恒成立（如 `d6!>1`）的前置守卫，防止爆炸链永远无法终止。
- 修复骰子修饰符（`kh`/`kl`、`!`、`r`/`ro`、`s` 等）书写顺序固定导致部分组合（如 `4d6r<2kh3`）漏解析的问题：改为循环应用各子解析器直至收敛，不再限定顺序。
- 修复 `d6!=` 等残缺比较点（有比较符无数字）被静默当作 `d6!` 吞掉的问题：整体回退到消耗比较符之前的位置，交由上层报出明确错误。
- 修复优势/劣势（`adv`/`dis`）在显式骰数 >2 时静默覆盖为 2 颗骰子、掩盖用户意图的问题：现直接报错提示。
- 修复爆炸追加骰被重骰后，历史展示丢失中间重骰记录的问题。
- 修复 `RollHistoryManager.clear()` 未加锁、可能与并发 `add()` 交错导致"已清空的历史被在途写入复活"或新记录被静默丢弃的问题。
- 修复 `RollHistoryManager` 曾按会话 key 建立独立锁、锁字典永不回收导致长期运行内存缓慢泄漏的问题：改为单把管理器级锁，历史操作争用可忽略，内存占用有界。
- 修复 `_safe_int` 缺少上限校验、手改配置文件可绕过 WebUI 滑块上限的问题。
- 修复 `_safe_bool` 兜底分支静默吞掉异常、故障难以排查的问题：改为先记录 `logger.warning` 再回退默认值。
- 修复 LLM 函数工具 `roll_dice` 的 `expression` 参数无默认值导致 LLM 省略该参数时直接 `TypeError` 的问题：AstrBot 生成的工具 schema 不含 `required` 列表，所有参数对 LLM 均为可选，现补充默认值并在省参时退化为掷当前会话默认骰。

### 新增
- LLM 函数工具 `roll_dice` 新增 `dc`（number）参数：可直接传入难度等级，无需再依赖"标签 数字"的隐式约定。
- 新增 pytest 单元测试套件（90+ 用例），覆盖配置辅助函数、解析器、掷骰引擎、格式化、历史记录及 LLM 工具辅助函数。

### 文档
- 新增 README「路线图 / 可拓展方向」章节，记录角色卡落地、重复掷骰、先攻追踪器等 8 个后续可拓展方向。
- 更新 README 语法参考中穿透爆炸、修饰符顺序、adv/dis 骰数限制、恒成立爆炸条件、LLM 工具参数等相关表述，修正与当前实现不一致之处。
- 部分内部注释归位与措辞修正。

## [0.6.1] - 2026-03-01

### 修复
- 修复 `/rh clear` 命令在 `enable_whitelist=False` 时群聊内任意用户均可清空历史的权限漏洞：新增独立的 `_check_history_clear_permission()` 逻辑，关闭白名单时群聊回退到 AstrBot 管理员判断，而非全放行。
- 修复 `RollHistoryManager.add()` 中读-改-写非原子导致并发投掷时历史条目互相覆盖的问题：引入按会话 key 的 `asyncio.Lock`，保证同一会话的写操作串行执行。
- 修复昵称、投掷表达式及结果中包含换行符等控制字符时可伪造历史行的文本注入风险：新增 `_sanitize()` 函数，在写入存储前和展示时双重剔除 `\r`、`\n`、`\t` 及其他非打印控制字符。

## [0.6.0] - 2026-03-01

### 新增
- 新增会话级投掷历史记录功能（`history.py` 模块）：每次投掷结果自动保存，支持按会话独立存储，可记录投掷表达式、结果摘要、发送者昵称与时间戳。
- 新增 `/rh` / `/rhistory` 命令：查看当前会话的投掷历史，群聊默认展示所有人的记录（含昵称），私聊仅展示自己的记录。
- 新增 `/rh me` 子命令：在群聊中筛选并展示当前用户自己的投掷历史。
- 新增 `/rh clear` 子命令：清除当前会话的全部历史记录，群聊中仅限白名单用户操作。
- 自定义触发前缀同步支持 `{prefix}rh` / `{prefix}rhistory` 命令。
- 新增 `enable_history` 配置项（默认 `true`）：全局启用或关闭投掷历史记录功能。
- 新增 `allow_view_history` 配置项（默认 `true`）：控制是否允许用户查看历史记录。
- 新增 `max_history_count` 配置项（默认 50，范围 5–500）：限制每个会话的最大历史条数，超出后自动淘汰最旧记录。

### 修复
- 修正 `metadata.yaml` 及 `README.md` 中插件描述里"掷骰"到"骰子"的术语不一致问题。

### 文档
- 更新 README，新增 `/rh` 系列命令说明、"投掷历史记录"功能章节及新增配置项文档。

## [0.5.1] - 2026-03-01

### 修复
- 修复 `CharacterSheet.__post_init__` 中 `skill_proficiencies` / `save_proficiencies` 未对 `None` 输入做兜底的问题；外部传入 `None`（插件生态中常见）时不再触发 `TypeError`，改为静默视为空集合。
- 修复 `_strip_label` 中 `Sm`/`Sc`/`Sk` 类 Unicode 符号（如数学运算符 `×`、`÷`、货币符号）会无条件截断表达式的问题；新策略：`L*` 自然语言文字与 `So`（Emoji）无条件截断为标签起点，`Sm`/`Sc`/`Sk` 仅在前有空白时才截断，避免粘贴进来的 Unicode 运算符被静默丢弃后给出难以理解的解析错误。
- 修复 `_apply_rerolls` 中多重重骰条件处理不完整的问题：移除首条命中后立即 `break` 的逻辑，改为按条件链顺序继续对更新后的骰值依次检查后续条件，实现完整多重重骰修饰语义。
- 修复非 `once` 重骰在深度耗尽后静默保留"仍命中条件"的值、缺乏可解释性的问题：深度耗尽且最终值仍落在应重骰区间时，骰子状态标记为 `kept_capped`，格式化层在骰值后追加 `?` 后缀（如 `3?`）提示用户该值未能成功重出。

## [0.5.0] - 2026-03-01

### 新增
- 新增 `max_input_len` 配置项：单次掷骰表达式允许的最大字符数，可在管理面板通过滑块调节（10–1000，默认 200），替代原先硬编码的 200 字符上限。
- 新增全角字符自动归一化（`_normalize_fullwidth`）：用户输入的全角运算符（如 ＋、－、（、））自动转换为对应 ASCII 字符，不再被误识别为标签分隔触发点。
- 新增净成功/失败计数格式化展示：成功计数模式下输出同时显示成功数、失败数及净值（如 `3成功 1失败 净+2`）。
- 新增爆炸追加骰与保留骰在成功计数模式下的 `*`/`x` 标注，展示更直观。
- 新增 `max_total_rolled` 参数限制单次掷骰实际产生的骰子总数上限（默认 500），防止多骰池加高爆炸深度组合生成超大列表。
- 新增前缀缓存 TTL 机制（300 秒）：管理面板修改全局或会话前缀后，缓存在 TTL 到期时自动刷新，无需重启插件。
- 新增有界 LRU 缓存用于会话级自定义触发前缀（上限 512 条）：提取专用 `_set_prefix_cache` 方法，驱逐逻辑更清晰。
- 新增有界 LRU 缓存用于角色卡内存管理，防止长时间运行时角色卡数据无限堆积。

### 修复
- 修复骰子面数/数量超出上限时错误信息表述不清晰的问题，提示中现明确显示实际值与限制值。
- 修复重骰历史记录追踪不完整的问题：补全中间重骰值的完整链路记录；保留骰组按值排序输出。
- 修复骰子表达式解析中不可识别修饰符（如拼写错误的关键词）未给出明确报错的问题，新增边界守卫防止解析器进入无限回退循环。
- 修复标签分离逻辑（`_strip_label`）中含运算符的纯 ASCII 表达式（如 `2d6 + 1d4 伤害`）被空格误切的问题：空格后紧跟 `+`/`-` 时整体去空格视为算式，再从下一个非算式位置切分标签。
- 修复非 ASCII 字符判断逻辑，改进表达式与标签的边界识别。
- 修复权限检查和输入校验逻辑，`/dset`、`/rprefix` 等命令对边界条件（空输入、非法符号等）处理更健壮。
- 修复 `/rprefix "/"` 拒绝逻辑：将 `/` 设为前缀会与系统默认前缀重叠，现予以明确拒绝。

### 重构
- 重构 `_annotate_die` 标注逻辑，引入按位置精确标注的 `die_rolls` 列表，删除约 45 行遗留频率计数回退代码。
- 重构 `is_natural_20`/`is_natural_1` 检测为共享方法 `_check_natural_roll`，消除重复判断逻辑。
- 重构 `formatter.py` 比较函数：将私有 `_compare` 替换为已公开的 `_cmp`，统一比较逻辑入口。
- 将 `_MAX_INPUT_LEN` 从函数内局部变量提升为模块级常量，便于配置层引用和覆盖。
- `CharacterSheet` 属性分值 Clamp 及等级限制改用显式 `_clamp()` 函数调用，替代原 `getattr`/`setattr` 循环；`CharacterManager` 加入 `asyncio.Lock` 保证并发安全。
- 全面将代码注释翻译为中文，提升可读性与维护一致性。

### 文档
- 更新 README，补充白名单字段说明及标签格式化规则说明。

## [0.4.0] - 2026-02-25

### 新增
- 新增自定义指令触发前缀支持：支持在管理面板设置全局默认前缀，或通过 `/rprefix` 命令为当前会话独立设置触发前缀（如 `.`、`!` 等），与系统默认前缀并存，不互相替换。
- 新增 `/rprefix` 命令：支持查询、设置、重置当前会话的自定义触发前缀；设置后 `{prefix}r`、`{prefix}roll`、`{prefix}dset` 等均可触发对应功能。
- 新增 `default_cmd_prefix` 配置项：全局默认额外触发前缀，留空则仅响应系统默认前缀。
- 新增 `allow_custom_prefix` 配置项：控制是否允许用户通过 `/rprefix` 命令设置会话级触发前缀。
- 自定义前缀与 `/dset` 共用同一套白名单配置（`enable_whitelist`、`whitelist_users`、`allow_private_bypass_whitelist`）。

### 文档
- 更新 README，补充 `/rprefix` 命令说明及自定义触发前缀相关配置项文档。

## [0.3.0] - 2026-02-25

### 新增
- 新增 `/dset` / `/dice_set` 命令：支持为当前会话（群聊或私聊）独立设置默认骰面数，设置后 `/r` 无参数时将投掷指定面数的骰子。
- 新增 `default_dice_sides` 配置项：全局默认骰面数（初始为 20），无参数执行 `/r` 时使用。
- 新增 `allow_session_dice_sides` 配置项：设为 `false` 后全局禁用 `/dset` 命令。
- 新增 `enable_whitelist`、`whitelist_users`、`allow_private_bypass_whitelist` 配置项：对 `/dset` 命令进行白名单权限控制，私聊可配置为绕过限制。

### 修复
- 修复 `/dset` 无参数查询时，响应中默认骰面数来源（会话设置 / 全局默认）描述不清晰的问题。

### 文档
- 更新 README，新增骰面大小设置与权限控制说明。

## [0.2.1] - 2026-02-25

### 新增
- 新增配置值安全转换函数 `_safe_int` 与 `_safe_bool`，防止管理面板填入非法值时导致插件加载失败。
- 新增角色卡管理器（`CharacterManager`）懒加载机制，避免核心持久化接口未实现时被意外调用。
- 新增 MIT 许可证文件。

### 修复
- 修复 `d20dis`（劣势骰语法糖）解析异常的问题：`dis` 后缀现可被正确识别为 `kl1` 简写。
- 修复骰池表达式解析中的边界处理逻辑，新增输入长度限制（最长 200 字符），防止超长表达式造成性能问题。
- 修复插件在 AstrBot 插件市场中显示名称与描述不准确的问题。

### 优化
- 重构掷骰执行引擎，完善内部文档注释，提升代码可读性与可维护性。
- 修正修正值（modifier）计算逻辑，修复负修正值运算异常。
- 角色卡 `AbilityScores` 属性分值 Clamp 至 [1, 30]，`CharacterSheet` 等级 Clamp 至 [1, 20]，规范化熟练项集合类型。

### 文档
- 更新 README，改进指令格式说明与排版清晰度。

## [0.2.0] - 2026-02-25

### 新增
- 初始版本发布。
- 支持完整 Roll20 骰池语法：基础骰（`d20`、`2d6+5`）、FATE/Fudge 骰（`4dF`）、保留最高/最低（`4d6kh3`、`2d20kl1`）、丢弃最高/最低（`8d6d3`、`8d6dh3`）、优势/劣势（`d20adv`、`d20dis`）。
- 支持三种爆炸变体：标准爆炸（`d6!`）、复合爆炸（`5d6!!`，Shadowrun 风格）、穿透爆炸（`5d6!p`，HackMaster 风格）；支持自定义爆炸阈值（`d6!>4`）。
- 支持目标数成功计数（`3d6>3`）与失败计数（`3d6>3f1`）。
- 支持重骰：循环重骰（`2d8r<2`）与单次重骰（`2d6ro<2`）。
- 支持结果排序：升序（`8d6s`）与降序（`8d6sd`）。
- 支持多骰组合与修正值（`2d6+1d4+3`）。
- 支持标签注释（`d20 攻击检定`、`d20#攻击检定`）与 DC 技能检定判定，天然 20 强制大成功、天然 1 强制大失败。
- 注册 LLM 函数工具 `roll_dice`，Agent 模式下 LLM 可在 TRPG 叙事中主动发起掷骰。

### 优化
- 重构修正值计算逻辑，修复负修正值运算问题。
